% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_model.R
\name{cell_grid_write}
\alias{cell_grid_write}
\title{Record a database's grid, so tile pruning cannot guess it wrong}
\usage{
cell_grid_write(con, grid_id)
}
\arguments{
\item{con}{a writable DBI connection to a serving database}

\item{grid_id}{grid registry key, e.g. \code{"usa05"} or \code{"global05"}}
}
\value{
the written data frame, invisibly
}
\description{
\code{\link[=cell_grid_ncol]{cell_grid_ncol()}} resolves the grid width from a \code{cell_grid} table in the database
and otherwise falls back to v8's 7200. \strong{No release ever wrote that table}, so the
fallback was always taken — correct for \code{global05} by coincidence and wrong for every
\code{usa05} release. The consequence was silent and total: \code{cell_model} is Hive-partitioned
by a tile id computed from the grid width, so a wrong width computes a \emph{different, valid}
tile id, \verb{WHERE tile IN (…)} prunes away the only partition holding the cells, and the
clicked-cell species list came back \strong{empty} on v1-v7 rather than failing. Measured on
the promoted release: a v7 cell with 477 models returned 0 species.
}
\details{
Call this when building a serving database, before \code{\link[=cell_model_tile_check]{cell_model_tile_check()}}.
}
\concept{calc}
