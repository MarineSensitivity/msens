% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{parquet_digest}
\alias{parquet_digest}
\title{Content digest of a Parquet table or dataset}
\usage{
parquet_digest(path, con = NULL)
}
\arguments{
\item{path}{a \code{.parquet} file, a directory of them, or a glob}

\item{con}{optional DuckDB connection to reuse}
}
\value{
a one-row data frame: \code{n_rows}, \code{n_groups} (distinct rows),
\code{n_files}, \code{bytes}, \code{schema}, \code{schema_digest}, \code{data_digest}, \code{digest}
}
\description{
A hash of what the table CONTAINS, independent of row order, file mtime,
file count and partitioning — see the note at the top of \code{sync.R} for why
byte comparison cannot do this job.
}
\concept{sync}
