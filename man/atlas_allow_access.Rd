% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{atlas_allow_access}
\alias{atlas_allow_access}
\title{Which \code{access} values THIS app instance may show}
\usage{
atlas_allow_access(preview = atlas_is_preview())
}
\arguments{
\item{preview}{is this the preview instance? Default reads \code{MS_PREVIEW}}
}
\value{
character vector to pass as \code{allow_access} to \code{\link[=atlas_resolve_ver]{atlas_resolve_ver()}}
}
\description{
The one place the preview policy lives. The public Shiny Server instance
(\code{MS_PREVIEW} unset) may resolve \code{public} versions only; the preview instance
-- a second Shiny Server \code{server} block whose wrapper \code{app.R} sets
\code{MS_PREVIEW=1} and is reachable only through the signed-in
\code{preview.marinesensitivity.org} vhost -- may resolve everything. Enforcement is
by \emph{process}, not by a trusted header: Shiny Server OSS opens its own
websocket to the R worker, so no proxy header ever reaches \code{session$request},
and a client-supplied \code{url_search} could otherwise steer the shared instance
to a restricted release.
}
\concept{version}
