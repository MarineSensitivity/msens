% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget.R
\name{widget_png}
\alias{widget_png}
\title{Render an htmlwidget to a static PNG}
\usage{
widget_png(widget, file, width = 1200, height = 800, delay = 8, zoom = 2)
}
\arguments{
\item{widget}{an htmlwidget (e.g. from \code{mapgl::maplibre()}, \code{mapview}, \code{leaflet})}

\item{file}{output PNG path, relative to the notebook (parent dirs created)}

\item{width, height}{viewport size in CSS pixels}

\item{delay}{seconds to wait before the screenshot — map tiles and GeoJSON
layers load asynchronously, so too short a delay yields a blank basemap}

\item{zoom}{device pixel ratio; 2 gives a retina-quality PNG}
}
\value{
\code{file}, invisibly-safe for \code{knitr::include_graphics()}
}
\description{
Saves \code{widget} to a temporary self-contained-free HTML, screenshots it with
headless Chrome via \code{webshot2}, and returns the image path — for use as
\code{knitr::include_graphics(widget_png(m, "figures/map.png"))} in a notebook
chunk, in place of printing the widget.
}
\details{
Use it for maps whose payload is large and whose interactivity is not the
point. Keep printing the widget where panning/zooming genuinely matters.
}
\examples{
\dontrun{
knitr::include_graphics(widget_png(m_gap, "figures/gap_turtles.png"))
}
}
\concept{viz}
