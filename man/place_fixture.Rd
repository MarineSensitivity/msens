% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{place_fixture}
\alias{place_fixture}
\title{Read a place fixture}
\usage{
place_fixture(id)
}
\arguments{
\item{id}{fixture id (file basename without \code{.json}), or a path to one}
}
\value{
a list with \code{id}, \code{rule}, \code{grid}, \code{geometry} (an \code{sfc}) and \code{expected}
(a tibble of \code{cell_id}, \code{pct})
}
\description{
The fixtures in \verb{inst/fixtures/places/} are the cross-language contract for
\code{\link[=cells_in_polygon_grid]{cells_in_polygon_grid()}}: each carries a polygon, the grid spec it was computed
on, and the expected \verb{(cell_id, pct)}. R and TypeScript read the same bytes, so
neither side can quietly adopt its own rounding or antimeridian rule.
}
\concept{place}
