% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{registry_merge}
\alias{registry_merge}
\title{Merge a freshly built asset registry with the one already published}
\usage{
registry_merge(
  new,
  prior,
  allow_shrink = FALSE,
  key = c("ds_key", "asset_type", "representation")
)
}
\arguments{
\item{new}{registry rows built by this run (data frame; may be zero-row)}

\item{prior}{registry rows currently published (data frame, \code{NULL}, or zero-row)}

\item{allow_shrink}{write \code{new} verbatim — no carry-forward, no shrink check}

\item{key}{columns defining a class}
}
\value{
\code{new} plus any carried-forward rows, with the carried class labels in the
\code{"carried"} attribute and the per-class counts in \code{"classes"}
}
\description{
\code{native_asset} is a registry of what is \strong{published}, but \code{publish_native.qmd}
assembles it from whatever a given run happened to build, then writes it with
\code{overwrite = TRUE}. A run that skips a build chunk (\code{NATIVE_SKIP_PMTILES}, or an
opt-in class such as \code{PUBLISH_MERGED_COG} left off) therefore rewrote the table
from its own products alone, silently deleting every row of the classes it did not
touch. That is exactly how all 2,234 vector-range PMTiles rows disappeared from the
published v8 registry while the tiles themselves sat untouched on S3 and the file
host: the species app stopped offering any range input, and the content-hash
checkpoint recorded the loss as a clean run (\code{n_pmtiles_native: 0}).
}
\details{
A \strong{class} is one \verb{(ds_key, asset_type, representation)} triple — the unit the app
actually reasons about, since that is what becomes an input layer. This function:
\itemize{
\item \strong{carries forward} every prior class the new registry has no rows for at all
(the skipped-chunk case: those assets are still published, the run just did not
enumerate them); and
\item \strong{errors} when a class the new registry \emph{does} cover came back smaller than it
was — a partial build masquerading as a complete one, which carrying forward
would paper over with a mix of fresh and stale rows.
}

Deliberate removals stay possible but must be explicit: \code{allow_shrink = TRUE}
(wired to \code{NATIVE_REGISTRY_REBUILD=1}) writes exactly what the run produced.
}
\concept{publish}
