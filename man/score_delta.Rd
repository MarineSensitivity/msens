% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{score_delta}
\alias{score_delta}
\title{Join two versions' scores and compute per-key deltas (pure)}
\usage{
score_delta(
  df_a,
  df_b,
  key = "programarea_key",
  value = "score",
  labels = c("a", "b")
)
}
\arguments{
\item{df_a, df_b}{data frames each with columns \code{key} and \code{value}}

\item{key}{join column name (default \code{"programarea_key"})}

\item{value}{value column name (default \code{"score"})}

\item{labels}{length-2 suffixes for the two versions (default \code{c("a","b")})}
}
\value{
a tibble with \code{key}, \verb{<value>_<labels[1]>}, \verb{<value>_<labels[2]>},
\code{delta}, sorted by descending \code{abs(delta)}
}
\description{
Inner-joins two score tables on \code{key} and returns the paired values plus
\code{delta = value_b - value_a}. Pure (data-frame in, data-frame out) so it can be
unit-tested without a database.
}
\concept{validate}
