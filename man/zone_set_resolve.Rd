% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zone_set.R
\name{zone_set_resolve}
\alias{zone_set_resolve}
\title{Resolve a release's zone columns to zone-set keys via the registry}
\usage{
zone_set_resolve(ver, fld, zone_sets)
}
\arguments{
\item{ver}{MST version, e.g. \code{"v7"}}

\item{fld}{zone key column(s) from the release's \code{zone} table}

\item{zone_sets}{the registry (\code{data/zone_sets.csv})}
}
\value{
character vector of \code{zone_set_key}, \code{NA} where unresolvable
}
\description{
Only v8 stamps \code{zone_set_key} into its own \code{zone} table; v1–v7 identify a
spatial unit by \code{fld} (\code{programarea_key}) and a per-version table name
(\code{ply_programareas_2026_v7}). This maps the former to the vintage that release
actually used, by matching \code{zone_type} and finding \code{ver} in the registry's
space-separated \code{versions} column.
}
\details{
Returns \code{NA} rather than guessing when a release's zone type is absent from
the registry, or when the registry lists more than one vintage of that type
for the same version. A wrong outline is worse than a missing one: it would
draw the 2026 Program Areas over scores computed on a different geometry and
look entirely plausible.
}
\concept{zone_set}
