% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc.R
\name{build_zone_taxon}
\alias{build_zone_taxon}
\title{Build the precomputed zone x taxon summary table}
\usage{
build_zone_taxon(con, overwrite = TRUE)
}
\arguments{
\item{con}{a DBI connection to the FULL \code{sdm.duckdb} (local \code{model_cell})}

\item{overwrite}{replace an existing \code{zone_taxon} table}
}
\value{
invisibly, the number of rows written
}
\description{
Computes \code{\link[=species_for_zone]{species_for_zone()}} for \strong{every} zone in the \code{zone} table and
writes the result as a single \code{zone_taxon} table.
}
\details{
WHY PRECOMPUTE. v7 shipped a \code{zone_taxon} table and v8 dropped it, on the
assumption the app could aggregate live. It can locally — but \strong{not on the
server}, which holds only the KB-sized \code{serve.duckdb} whose \code{model_cell} is a
view over S3 Parquet \emph{partitioned by \code{mdl_id}} for per-model point reads
(titiler tiles). A zone-wide aggregation there means listing and scanning the
whole 580M-row dataset over HTTPS; in practice it fails outright with
\verb{IO Error: ... HTTP GET .../serve/model_cell/}. Precomputing here — where
\code{model_cell} is local — turns that into a few-MB table the app just reads.
}
\concept{calc}
