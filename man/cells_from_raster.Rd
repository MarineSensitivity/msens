% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{cells_from_raster}
\alias{cells_from_raster}
\title{Resample a raster SDM onto the global 0.05° cell grid}
\usage{
cells_from_raster(
  r,
  cellid_tif,
  method = "bilinear",
  min_value = 1,
  zero_fill = TRUE
)
}
\arguments{
\item{r}{a \code{SpatRaster} (single layer) in EPSG:4326}

\item{cellid_tif}{path to the global cell-id COG}

\item{method}{resampling method (default "bilinear"; use "near" for categorical)}

\item{min_value}{drop resampled cells below this (default 1)}

\item{zero_fill}{zero-fill NA within the source extent before resampling so the
surface fades to 0 at its edge (default TRUE)}
}
\value{
a tibble \verb{(cell_id integer, value double)}
}
\description{
For native raster SDMs on a \emph{different} grid than the v8 0.05° (e.g. NCCOS
density COGs). Zero-fills absent cells within the source extent then bilinear-
resamples onto the cell grid (the same fade as AquaMaps) and returns
\verb{(cell_id, value)} for cells at/above \code{min_value} — the source's own values
define coverage (no land mask; a marine SDM simply has no land values). A source
already on the v8 topology (AquaX, Bio-Oracle) needs no resample — read it
against \code{cellid_tif} directly.
}
\concept{ingest}
