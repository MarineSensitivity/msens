% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_validate}
\alias{app_validate}
\title{Validate an object against its published \verb{app/} schema}
\usage{
app_validate(x, name, what = name)
}
\arguments{
\item{x}{the object (a list, as \code{\link[jsonlite:toJSON]{jsonlite::toJSON()}} would see it)}

\item{name}{schema name, see \code{\link[=app_schema_path]{app_schema_path()}}}

\item{what}{label used in the error message}
}
\value{
\code{x}, invisibly; errors with the failing JSON pointers
}
\description{
Every builder validates its own output before it is written, so a shape change
fails in the notebook that made it rather than in a browser three steps later.
The schemas are \code{additionalProperties: false} at the top level on purpose: a key
the contract does not name is a key the app will not read, and shipping it is how
a bundle quietly grows.
}
\concept{app}
