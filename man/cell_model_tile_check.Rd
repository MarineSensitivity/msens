% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_model.R
\name{cell_model_tile_check}
\alias{cell_model_tile_check}
\title{Assert a database's stored tile ids match the width it reports}
\usage{
cell_model_tile_check(con, n = 20L)
}
\arguments{
\item{con}{a DBI connection to a serving database with a \code{cell_model} table/view}

\item{n}{how many cells to sample}
}
\value{
\code{TRUE}, invisibly; errors on mismatch
}
\description{
The one check that \strong{cannot pass on a mismatch}: it recomputes the tile id for cells
taken from \code{cell_model} itself and compares them with the ids stored beside those cells.
Counting rows, listing views, or querying a tile cannot distinguish a right width from a
wrong one — a wrong tile id is still a valid tile id, so the query simply returns nothing.
}
\concept{calc}
