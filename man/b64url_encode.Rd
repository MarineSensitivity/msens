% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{b64url_encode}
\alias{b64url_encode}
\alias{b64url_decode}
\title{Base64url, RFC 4648 section 5, no padding}
\usage{
b64url_encode(raw)

b64url_decode(s)
}
\arguments{
\item{raw}{a raw vector}

\item{s}{a base64url string}
}
\value{
the encoded string / the decoded raw vector
}
\description{
Not \code{base64enc}: that emits the standard \verb{+/} alphabet with \code{=} padding, and the
three-character fixup afterwards is exactly the kind of step that silently
survives a round trip in one language and not the other. The alphabet is checked
on the way in, so \code{+}, \code{/} and \code{=} are a named rejection rather than a stray byte.
}
\examples{
b64url_encode(as.raw(c(0x10, 0x03)))
b64url_decode("EAM")
}
\concept{app}
