% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{.metric_short_label}
\alias{.metric_short_label}
\title{A human short label for a metric key}
\usage{
.metric_short_label(key)
}
\arguments{
\item{key}{one or more \code{metric_key} values}
}
\value{
a character vector of the same length, one short label per key (the key
itself for a shape this function does not recognise — the same fallback the
long-label helper \code{.metric_label()} already uses)
}
\description{
Parity audit 2026-09-24 (item M1's sibling: zone names + short labels): the
manifest's \verb{metrics[].label} is the field the Atlas app's
\code{metricLabelsFromManifest()} (\code{src/lens/scores/boot.ts}) reads for the legend
title and layer picker (\verb{boot.layers[].label} is deliberately the LONG
description — see \code{\link[=app_boot]{app_boot()}}'s \code{layers} construction). Left to a
hand-curated \verb{layers_\{ver\}.csv}, that field has published the bare metric-key
fragment \code{"score"} for the composite and \code{"prim prod, ecorgn"} for
\code{primprod_ecoregion_rescaled} — this is the canonical, version-independent
fallback \code{\link[=manifest_build]{manifest_build()}} backfills with whenever a release's own curation
leaves a cell-scored metric unlabelled.
}
\keyword{internal}
