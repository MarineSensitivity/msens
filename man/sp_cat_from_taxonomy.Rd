% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{sp_cat_from_taxonomy}
\alias{sp_cat_from_taxonomy}
\title{Species category (\code{sp_cat}) from taxonomy}
\usage{
sp_cat_from_taxonomy(
  class,
  kingdom = NA_character_,
  is_botw = FALSE,
  is_turtle = FALSE
)
}
\arguments{
\item{class}{WoRMS class (character; \code{NA} allowed)}

\item{kingdom}{WoRMS kingdom (character; \code{NA} allowed)}

\item{is_botw}{logical; the taxon is keyed by BirdLife (BOTW)}

\item{is_turtle}{logical; the taxon is one of the SWOT sea turtles}
}
\value{
character vector of categories, parallel to \code{class}
}
\description{
The one rule that turns a taxon's WoRMS class / kingdom (plus "is a BirdLife bird" and "is a
SWOT turtle") into the MST scoring component. It lived inline in \code{merge_taxon.qmd}; from v9
the AquaX ingest also needs it — to report species by component \emph{before} a merge exists —
so it is a function both call rather than two copies that drift.
}
\details{
Categories: \code{turtle} (the SWOT set), \code{bird} (BirdLife or class Aves), \code{mammal}, \code{reptile} and
\code{amphibian} (categorized but EXCLUDED from scoring), \code{fish}, \code{coral} (Hexa/Octocorallia),
\code{primary_producer} (Plantae / Chromista / Bacteria), else \code{invertebrate}. No \code{other}.
}
\examples{
sp_cat_from_taxonomy(c("Mammalia", "Teleostei", "Hexacorallia", NA),
                     c("Animalia", "Animalia", "Animalia", "Plantae"))
}
\concept{taxa}
