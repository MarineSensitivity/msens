% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_model.R
\name{cell_model_tiles}
\alias{cell_model_tiles}
\title{Spatial tile ids covering a set of cells}
\usage{
cell_model_tiles(cell_id, ncol = .CELL_GRID_NCOL)
}
\arguments{
\item{cell_id}{integer vector of cell ids}

\item{ncol}{grid width in columns; MUST match what the writer used. Pass
\code{cell_grid_ncol(con)} when reading a database rather than assuming.}
}
\value{
sorted unique integer tile ids
}
\description{
R-side twin of \code{\link[=cell_model_tile_sql]{cell_model_tile_sql()}}. Use it to add a \verb{tile IN (…)} filter
so DuckDB prunes to the relevant partitions — \strong{without it a query reads every
partition}, which is the whole problem \code{cell_model} exists to solve.
}
\examples{
cell_model_tiles(c(1080221L, 1080222L))
}
\concept{calc}
