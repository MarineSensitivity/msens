% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{atlas_access_default}
\alias{atlas_access_default}
\title{Default \code{access} for a version whose registry row does not say}
\usage{
atlas_access_default(status)
}
\arguments{
\item{status}{vector of \code{released} / \code{prerelease} / \code{retired}}
}
\value{
character vector of \code{public} / \code{restricted}
}
\description{
A pre-release is \code{restricted} (under review; reachable only through the
signed-in preview host), everything else is \code{public}. Used by
\code{\link[=atlas_versions]{atlas_versions()}} when \code{versions.json} predates the \code{access} field, and by
the registry writer to fill the column, so the two cannot disagree.
}
\concept{version}
