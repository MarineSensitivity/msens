% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{manifest_build}
\alias{manifest_build}
\title{Build a version manifest from that release's database}
\usage{
manifest_build(
  con,
  ver,
  status = "released",
  access = atlas_access_default(status),
  grid_id = grid_for_ver(ver),
  base = atlas_base_url(),
  metrics = NULL,
  capabilities = list(),
  zone_tiles = list(),
  zone_sets = NULL,
  extra = list()
)
}
\arguments{
\item{con}{open DuckDB connection to that release's database}

\item{ver}{version label}

\item{status}{\code{released}, \code{prerelease} or \code{retired}}

\item{access}{\code{public} or \code{restricted}; defaults from \code{status} via
\code{\link[=atlas_access_default]{atlas_access_default()}}, and is copied into the manifest so a release
describes its own visibility}

\item{grid_id}{defaults to \code{\link[=grid_for_ver]{grid_for_ver()}}}

\item{base}{atlas base URL from \code{\link[=atlas_base_url]{atlas_base_url()}}}

\item{metrics}{optional data frame of published metric COGs to merge in
(\code{metric_key}, \code{subregion_key}, \code{cog}, \code{rescale_min}, \code{rescale_max}); until
score COGs are published the \code{score_cogs} capability stays FALSE}

\item{capabilities}{named logicals overriding the derived ones. Needed
whenever a surface is NOT a table in \code{con}: v8's \code{cell_model}, for instance,
is published as a Parquet directory beside the database, so deriving from
\code{con} alone would advertise \code{cell_species_list = FALSE} and switch off a
panel that works. Overrides must be justified by checking the RELEASE, not
by optimism.}

\item{zone_tiles}{named list of \code{zone_set_key} -> PMTiles URL, attached to
the zones table so an app resolves outlines by VINTAGE instead of a
hardcoded unversioned filename on the file host}

\item{zone_sets}{the zone-set registry (\code{data/zone_sets.csv}), used to resolve
\code{zone_set_key} for a release whose own \code{zone} table predates that column.
Only v8 stamps \code{zone_set_key} into the database; v1–v7 record a spatial unit
as \code{tbl}/\code{fld} only, so without the registry every historical manifest comes
out with no \code{zone_set_key} and therefore no \code{pmtiles} — the app then cannot
draw a single zone outline on any version but the newest.}

\item{extra}{named list merged into the manifest (e.g. \code{zone_sets})}
}
\value{
a validated manifest list
}
\description{
Introspects the release rather than being told about it, so a manifest cannot
drift from the data it describes. Everything that differs between releases —
which model id is public, which tables exist, which metrics are cell-level,
which spatial units were scored — is \emph{read}, not assumed.
}
\details{
Capabilities are derived from presence, and default to \strong{FALSE}: a release
that lacks \code{cell_model} must not advertise a per-cell species list.
}
\concept{version}
