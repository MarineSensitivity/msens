% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cog_store.R
\name{cog_store_index}
\alias{cog_store_index}
\title{Index the existing COG store}
\usage{
cog_store_index(
  grid_id = NULL,
  bucket = "s3://oceanmetrics.io-public/marine-atlas",
  aws = "aws"
)
}
\arguments{
\item{grid_id}{restrict to one grid's prefix, or \code{NULL} for the whole store}

\item{bucket}{S3 URI of the atlas root}

\item{aws}{path to the \code{aws} CLI}
}
\value{
character vector of \code{content_hash} values already present
}
\description{
ONE recursive listing into a set, so "is this already published?" is a lookup
rather than a HEAD request per model — at ~30k models per release the
per-object form is the difference between seconds and hours.
}
\concept{cog_store}
