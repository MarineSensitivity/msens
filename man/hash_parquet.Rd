% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{hash_parquet}
\alias{hash_parquet}
\title{Order-independent content fingerprint of Parquet file(s)}
\usage{
hash_parquet(glob, con = NULL, cols = NULL)
}
\arguments{
\item{glob}{a \code{read_parquet} glob/path (e.g. \code{"dir/*.parquet"}, \code{"dir/**/*.parquet"})}

\item{con}{optional DuckDB connection; a temp in-memory one is used if \code{NULL}}

\item{cols}{optional character vector of columns to hash (default: all)}
}
\value{
a 16-char hex fingerprint string
}
\description{
Scans the ON-DISK Parquet (cheap — never re-runs the ingest) and reduces it to
a short fingerprint invariant to row order, row-group boundaries, compression
and Parquet metadata timestamps. Works for surface files (\verb{mdl_key,cell_id,val})
and registry tables alike; columns are introspected unless given.
}
\concept{manifest}
