% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{.zone_where}
\alias{.zone_where}
\title{WHERE clause selecting one spatial unit's zones (internal)}
\usage{
.zone_where(con, fld, zone_set_key, lbl)
}
\arguments{
\item{con}{a DBI connection to an \code{sdm.duckdb}}

\item{fld}{zone field, e.g. \code{"programarea_key"}}

\item{zone_set_key}{optional \verb{\{zone_type\}_\{YYYY-MM\}}}

\item{lbl}{label used in the error message}
}
\value{
a length-1 glue string for use after \code{WHERE} (table alias \code{z})
}
\description{
Pins to \code{zone_set_key} when both asked for and available; a release predating
the column keeps the \code{fld} filter rather than silently comparing nothing, and
naming a zone set the release lacks is an error, not an empty join.
}
\keyword{internal}
