% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc.R
\name{species_for_zone}
\alias{species_for_zone}
\title{Species table aggregated across a zone}
\usage{
species_for_zone(con, zone_fld, zone_val)
}
\arguments{
\item{con}{a DBI connection to an \code{sdm.duckdb}}

\item{zone_fld}{zone field, e.g. \code{"programarea_key"}}

\item{zone_val}{zone value, e.g. \code{"GAA"}}
}
\value{
tibble, same shape as \code{\link[=species_for_cells]{species_for_cells()}}
}
\description{
One row per species within a named zone (\code{subregion_key}, \code{programarea_key}
or \code{ecoregion_key}), aggregated with \code{pct_covered} weighting.
}
\details{
Computed live from \code{zone_cell} + \code{model_cell} + \code{taxon} rather than read from
a precomputed table: v7 shipped a \code{zone_taxon} table, but \strong{v8 does not build
one}, which left the app's species table broken. Measured on the v8 database,
the largest zone (\code{subregion_key = "USA"}, ~349k cells, ~10k species) takes
~5 s, so precomputation is not required.
}
\concept{calc}
