% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interp.R
\name{model_hex_from_weights}
\alias{model_hex_from_weights}
\title{Interpolate one model's source values onto hexes with precomputed weights}
\usage{
model_hex_from_weights(
  con,
  model_vals,
  mdl_seq,
  threshold = 0,
  w_tbl = "hex_src_w"
)
}
\arguments{
\item{con}{DuckDB connection}

\item{model_vals}{data.frame(\code{src_i}, \code{value}) — the model's non-absent source
cells only (e.g. a species' occupied HCAF cells with suitability 1-100)}

\item{mdl_seq}{model id stamped on the output}

\item{threshold}{drop interpolated hex values below this (default 0)}

\item{w_tbl}{weight-table base name from \code{\link[=hex_grid_weights]{hex_grid_weights()}}}
}
\value{
a tibble(mdl_seq, hex_id, value)
}
\description{
Applies \code{\link[=hex_grid_weights]{hex_grid_weights()}} output to a model's (sparse) source values:
\verb{hex value = sum(w * value over present source neighbours) / w_total}. Absent
source cells contribute 0 to the numerator but are still counted in \code{w_total},
so the interpolated surface decays smoothly to 0 beyond the model's footprint.
}
\concept{interp}
