% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{ms_track_session}
\alias{ms_track_session}
\title{Hand the browser the session facts only the server knows}
\usage{
ms_track_session(session)
}
\arguments{
\item{session}{the Shiny \code{session} object}
}
\value{
the sent list, invisibly
}
\description{
The Shiny session token cannot be read in JavaScript, so the server pushes it
once at session start; the client then stamps it on every queued row (the
\code{session} column of \code{\link[=ms_log_header]{ms_log_header()}}). Call it at the top of the \code{server}
function, before any \code{\link[=ms_track]{ms_track()}} call, so no event is written without it.
}
\details{
The token is authoritative, the IP is only a \strong{fallback}: behind shiny-server
a session sees \verb{127.0.0.1}, so an \code{ip} already baked into the page by
\code{\link[=ga_js]{ga_js()}} wins and this one is ignored. Without that rule the websocket's
useless address would silently overwrite the good one a moment after the page
supplied it.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  msens::ms_track_session(session)
  ...
}
}
}
\concept{analytics}
