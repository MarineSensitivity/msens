% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{atlas_manifest}
\alias{atlas_manifest}
\title{A version's manifest — the contract between a release and the apps}
\usage{
atlas_manifest(ver = NULL, base = atlas_base_url(), refresh = FALSE)
}
\arguments{
\item{ver}{version (resolved through \code{\link[=atlas_resolve_ver]{atlas_resolve_ver()}} if \code{NULL}/\code{"latest"})}

\item{base}{atlas base URL from \code{\link[=atlas_base_url]{atlas_base_url()}}}

\item{refresh}{re-fetch instead of using the session cache}
}
\value{
a list, with the resolved version in \verb{$ver}
}
\description{
\code{{ver}/manifest.json} declares everything needed to render that version, so the
app holds no per-version knowledge and publishing v9 needs no app edit. Required
keys are asserted here rather than defaulted: a \strong{missing \code{capabilities} block
must fail loudly}, since defaulting an unknown capability to "supported" makes
the app promise a panel the data cannot fill.
}
\concept{version}
