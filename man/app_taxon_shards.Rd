% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_taxon_shards}
\alias{app_taxon_shards}
\title{Build the \verb{app/taxon/\{xx\}.json} shards}
\usage{
app_taxon_shards(con, ver)
}
\arguments{
\item{con}{a DBI connection}

\item{ver}{version label}
}
\value{
a named list \verb{shard -> validated shard object}
}
\description{
256 shards keyed by \code{sprintf('\%02x', trailing_integer(key) \%\% 256)} (no trailing
digits, \code{00}). Each holds the full card per taxon: ids, listings, validity, the
merged surface with a \strong{precomputed bbox}, and the normalised input edges with
their assets — no \code{ms_merge} self-edge, so an input count means the same thing on
every generation.
}
\concept{app}
