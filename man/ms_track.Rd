% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{ms_track}
\alias{ms_track}
\title{Send a tracking event from the Shiny server to the browser}
\usage{
ms_track(session, event, ...)
}
\arguments{
\item{session}{the Shiny \code{session} object}

\item{event}{event name, passed to \code{\link[=ms_event]{ms_event()}}}

\item{...}{named event parameters, passed to \code{\link[=ms_event]{ms_event()}}}
}
\value{
the payload, invisibly
}
\description{
Pushes an event over the session's existing websocket; the client-side
handler installed by \code{\link[=ga_js]{ga_js()}} forwards it to GA4 and queues it for the Sheet
log. \strong{Non-blocking} — no HTTP request is made on the R side, so a slow or
unreachable log endpoint can never stall a reactive.
}
\details{
Use it for facts only the server knows (the scientific name behind a picker
value, a report's parameters, a row count, an error); pure UI interactions are
better tracked client-side by \code{\link[=ga_js]{ga_js()}}'s delegated handlers.
}
\examples{
\dontrun{
ms_track(session, "select_species",
         mdl_key = input$sel_sp, scientific_name = sp$scientific_name)
}
}
\concept{analytics}
