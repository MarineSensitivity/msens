% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{pra_score_delta}
\alias{pra_score_delta}
\title{Program-Area composite-score delta between two version databases}
\usage{
pra_score_delta(
  con_a,
  con_b,
  metric_key = METRIC_SCORE_DEFAULT,
  labels = c("v7", "v8"),
  zone_set_key = NULL
)
}
\arguments{
\item{con_a, con_b}{DBI connections to the two versions' \code{sdm.duckdb}}

\item{metric_key}{composite metric key (default \link{METRIC_SCORE_DEFAULT})}

\item{labels}{length-2 version labels (default \code{c("v7","v8")})}

\item{zone_set_key}{optional \verb{\{zone_type\}_\{YYYY-MM\}} to pin the spatial unit;
errors if a connection carries the column but not that value}
}
\value{
a tibble from \code{\link[=score_delta]{score_delta()}} keyed by \code{programarea_key}
}
\description{
Reads the Program-Area composite score from two SDM DuckDB connections (e.g.
v7 on the cell grid and v8 on the hex grid) and returns the per-Program-Area
\code{\link[=score_delta]{score_delta()}}. Centralizes the query previously inlined in
\code{workflows/dev/build_v7.R} so \code{build_v8.R} and \code{validate_versions.qmd} share it.
}
\details{
Schema-adaptive: the score/key column is \code{value} in v7 and \code{val} in v8 (the
reserved-word rename), so the column name is resolved per connection via
\code{\link[=.value_col]{.value_col()}} rather than hard-coded — otherwise a v7↔v8 (or v8↔v8) comparison
errors with "Table z does not have a column named value".

Pass \code{zone_set_key} to pin the comparison to ONE spatial unit. Without it the
query selects \code{fld = 'programarea_key'} from each database and trusts that the
two releases meant the same polygons. Measured across every published gpkg,
they do — every program-area layer from v2 through v8 is one geometry — but
that is a fact about the data, not a guarantee, and BOEM's planning units keep
changing. Naming the zone set makes the assumption explicit and checkable;
databases predating the column (v1–v7) fall back to the \code{fld} filter.
}
\concept{validate}
