% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{supersede_sql}
\alias{supersede_sql}
\title{Supersession predicate: one suitability dataset replaces another inside a mask}
\usage{
supersede_sql(
  superseded = "am",
  taxa = "supersede",
  mask = "ax_mask",
  src = "mc"
)
}
\arguments{
\item{superseded}{ds_key whose cells are dropped inside the mask (default \code{"am"})}

\item{taxa}{relation holding \code{ms_merge_key} of the taxa to supersede (default \code{"supersede"})}

\item{mask}{relation holding the \code{cell_id}s of the superseding dataset's extent (default \code{"ax_mask"})}

\item{src}{alias of the relation being filtered, with \code{ds_key}, \code{ms_merge_key}, \code{cell_id}}
}
\value{
a SQL predicate (no leading \code{WHERE}) that is TRUE for rows to KEEP
}
\description{
From v9, AquaX (\code{ax}) is a newer model of the same thing as AquaMaps (\code{am}) — but it was
delivered only over the US study area (its own ocean mask, 586,276 cells; NOT the same set as
\code{cell.in_usa}, which has 53,818 deep Aleutian / NW-Hawaiian cells AquaX never modeled). The
rule is therefore \strong{per taxon, inside the mask}: for a taxon that has a superseding
model, the superseded dataset's cells that fall inside the mask are dropped from the merge
input; outside the mask (the rest of the world, and the uncovered US cells) the old dataset
carries on. Applied to BOTH output surfaces, since it filters the input they share.
}
\details{
Why a filter on the input rather than a \code{coalesce()} inside \code{\link[=merge_sql]{merge_sql()}}: a per-cell coalesce
would keep an AquaMaps cell wherever AquaX has \emph{no} value — i.e. exactly where the newer
model says the species is absent — which defeats the point of superseding it. Dropping the old
cells over the whole mask lets AquaX's absences be absences.

Which taxa are superseded is a TABLE the caller materializes (\code{taxa}, one \code{ms_merge_key} per
row), not derived here from "has a superseding model", because the policy question — does an
AquaX run that predicted \emph{no} US presence also supersede? (v9: no, \code{AX_ABSENT_SUPERSEDES})
— belongs in the notebook, where it is a flag and a table a reviewer can read.
}
\examples{
supersede_sql()
\dontrun{ DBI::dbGetQuery(con, paste("SELECT * FROM mc WHERE", supersede_sql(src = "mc"))) }
}
\concept{merge}
