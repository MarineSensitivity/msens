% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{grid_spec}
\alias{grid_spec}
\title{Grid spec for the global cell-id raster}
\usage{
grid_spec(r)
}
\arguments{
\item{r}{a terra SpatRaster of the cell-id COG (\code{r_cellid_global.tif})}
}
\value{
a list with \code{nc}, \code{nr}, \code{xmin}, \code{ymax}, \code{resx}, \code{resy}, \code{crs}
}
\description{
Plain-list description of the global 0.05° \link{-180,180} cell-id grid, so
\code{\link[=publish_cog]{publish_cog()}} can construct a windowed raster from a model's \code{cell_id}s
without serializing a terra SpatRaster across parallel workers. \code{cell_id} is
the row-major (top-left origin) 1-based cell index of this grid, so a cell's
row/col is pure arithmetic (no lookup against the COG).
}
\concept{publish}
