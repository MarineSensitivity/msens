% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{grid_spec_for}
\alias{grid_spec_for}
\title{Grid spec by id, for \code{\link[=publish_cog]{publish_cog()}}}
\usage{
grid_spec_for(grid_id, cellid_tif = NULL)
}
\arguments{
\item{grid_id}{one of \code{\link[=grid_registry]{grid_registry()}}\verb{$grid_id}}

\item{cellid_tif}{optional path to that grid's cell-id COG}
}
\value{
a list with \code{nc}, \code{nr}, \code{xmin}, \code{ymax}, \code{resx}, \code{resy}, \code{crs},
\code{grid_id}, \code{lon360}
}
\description{
Same plain-list shape \code{\link[=grid_spec]{grid_spec()}} returns from a raster, plus \code{grid_id} and
\code{lon360}. When the grid's cell-id COG is on hand its geometry is read from the
file (authoritative, and keeps already-published v8 COGs bit-comparable, since
the real raster carries ~6e-6 deg of float drift from the nominal values); the
registry values are the offline fallback and are asserted against the raster.
}
\concept{grid}
