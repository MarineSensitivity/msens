% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{build_targets_list}
\alias{build_targets_list}
\title{Build the targets list from \verb{msens:} frontmatter}
\usage{
build_targets_list(
  workflows_dir = here::here(),
  exclude = NULL,
  verbose = TRUE
)
}
\arguments{
\item{workflows_dir}{directory holding the pipeline \code{.qmd}s (default
\code{\link[here:here]{here::here()}})}

\item{exclude}{character vector of target names (or \code{.qmd} filenames) to drop
from the pipeline; excluded targets are also stripped from other targets'
dependency lists}

\item{verbose}{print the parsed workflow table (default \code{TRUE})}
}
\value{
a \code{list()} of \code{tar_target_raw()} objects for \verb{_targets.R}
}
\description{
Reads every pipeline notebook's \verb{msens:} block (via \code{\link[=parse_qmd_frontmatter]{parse_qmd_frontmatter()}})
and returns a \code{list()} of \code{targets::tar_target_raw()} objects for use as the
body of \verb{_targets.R}. Each target's body is a \code{{}} block whose leading
statements are bare references to its upstream target names (so \code{targets}
draws the DAG edges), followed by a \code{quarto::quarto_render()} of the notebook
and the \code{output} path (tracked with \code{format = "file"} for hash-based
invalidation).
}
\details{
\verb{dependency: [auto]} (typically the \code{release} caboose) resolves to every
\code{grid} + \code{ingest} target.
}
\examples{
\dontrun{
# in _targets.R:
library(targets)
library(msens)          # or devtools::load_all("../msens")
build_targets_list()
}
}
\concept{workflow}
