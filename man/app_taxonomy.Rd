% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_taxonomy}
\alias{app_taxonomy}
\title{The WoRMS hierarchy the Composition treemap joins, restricted to this release}
\usage{
app_taxonomy(con, ver, dir_out, taxonomy_csv = NULL)
}
\arguments{
\item{con}{a DBI connection to a release database}

\item{ver}{version label}

\item{dir_out}{the \verb{app/} directory}

\item{taxonomy_csv}{path to the WoRMS hierarchy CSV, or \code{NULL} to write nothing
(a release with no hierarchy on hand advertises none — nothing is invented)}
}
\value{
a list with \code{path}, \code{rows} and \code{taxa}, or \code{NULL} when nothing was written
}
\description{
Written by the NOTEBOOK until now, outside \code{app_bundle_build()} and therefore
outside \code{boot$tables} — so the browser had no digest for it and \strong{OPFS could
never invalidate it}: a cached taxonomy from a previous release stayed until
someone cleared site data by hand. An object the app reads is an object the
contract has to describe.
}
\details{
The CSV is an INPUT, not something msens goes looking for: the hierarchy is a
dated WoRMS export shared across releases
(\verb{apps/scores/data/taxonomic_hierarchy_worms_2025-10-30.csv}), and a builder that
guessed its path would silently publish whichever copy happened to be nearest.
}
\concept{app}
