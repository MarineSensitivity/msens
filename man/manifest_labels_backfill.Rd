% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{manifest_labels_backfill}
\alias{manifest_labels_backfill}
\title{Backfill blank metric short labels on a manifest}
\usage{
manifest_labels_backfill(m)
}
\arguments{
\item{m}{a manifest list (from \code{\link[=manifest_build]{manifest_build()}}, or
\code{jsonlite::fromJSON(simplifyVector = TRUE)} of a published \code{manifest.json})
whose \verb{$metrics} is a data.frame with at least \code{metric_key}}
}
\value{
the same manifest, with \verb{$metrics$label} backfilled. Returned unchanged
if \code{m} carries no \code{metrics}, or an empty one.
}
\description{
\code{\link[=manifest_build]{manifest_build()}} applies this rule to \verb{$metrics$label} while building a
manifest from scratch (E7, parity review 2026-09-24); exported here so a
release-side patch can apply the SAME rule to an ALREADY-PUBLISHED
\code{manifest.json} — read it back, backfill, \code{\link[=validate_manifest]{validate_manifest()}}, \code{PUT} — without
reaching into an internal \code{:::} function. The two call one shared
implementation, so they cannot drift apart.
}
\details{
A label is "blank" when the \code{metrics$label} column is absent, \code{NA}, or
whitespace-only — never when it already carries a curated value (e.g. a
hand-written \verb{layers_\{ver\}.csv} entry, even a terse one like \code{"score"}): this
function \strong{never overwrites} an existing label.
}
\examples{
m <- list(metrics = data.frame(metric_key = c("extrisk_bird", "score_x"),
                               label = c(NA, "score")))
manifest_labels_backfill(m)$metrics$label
}
\concept{version}
