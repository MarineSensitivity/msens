% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_model}
\alias{app_model}
\title{The \code{mdl_id} -> \code{mdl_key} mapping the \code{cell_model} join needs}
\usage{
app_model(con, ver, dir_out)
}
\arguments{
\item{con}{a DBI connection}

\item{ver}{version label}

\item{dir_out}{the \verb{app/} directory}
}
\value{
a list with \code{path} and \code{rows}, or \code{NULL} on a release without \code{mdl_id}
}
\description{
\code{cell_model} stores the compact integer \code{mdl_id} on v8+; every name in the app
comes from \code{mdl_key}. The mapping lived only in \code{{ver}/tables/model.parquet},
OUTSIDE \verb{app/} and outside \code{boot$tables}, so the browser fetched a 1.1 MB table
it could not cache-invalidate to resolve a click. This writes the three columns
the join actually needs.
}
\details{
\strong{v8+ only.} A release whose \code{model} table has no \code{mdl_id} joins \code{cell_model}
directly on \code{mdl_seq} and needs no mapping at all: it writes nothing and
advertises nothing (D11 — a release that cannot supply a capability simply does
not advertise it).
}
\concept{app}
