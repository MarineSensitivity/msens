% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_zone_names}
\alias{app_zone_names}
\title{Zone names from a release's spatial-unit geometry}
\usage{
app_zone_names(path, type)
}
\arguments{
\item{path}{a GeoPackage (or any \code{sf::st_read()}-able file) carrying
\verb{\{type\}_key}/\verb{\{type\}_name} columns, e.g. \code{data/zone_sets.csv}'s \code{source}}

\item{type}{the zone type (\code{"programarea"}, \code{"planarea"}, \code{"ecoregion"},
\code{"subregion"}), i.e. the \verb{\{type\}_key}/\verb{\{type\}_name} column prefix}
}
\value{
\code{data.frame(fld, key, name)}, one row per distinct key
}
\description{
\code{zone}/\code{zone_metric} never carry a human name — only the key a report picks from
(\code{"ALA"}), because that is all \code{score_zones.qmd} writes (\verb{CREATE TABLE zone (..., val VARCHAR, ...)}, no name column, on every generation v1–v9). The name ("Aleutian
Arc") lives on the GEOMETRY instead: every zone-set GeoPackage carries a
\verb{\{type\}_key}/\verb{\{type\}_name} column pair (verified on the real v1–v8 sources —
\code{programarea_key}/\code{programarea_name}, \code{planarea_key}/\code{planarea_name},
\code{ecoregion_key}/\code{ecoregion_name}, \code{subregion_key}/\code{subregion_name}).
}
\details{
A release-side caller reads it here and hands the lookup to \code{\link[=app_zones]{app_zones()}} /
\code{\link[=app_boot]{app_boot()}} / \code{\link[=app_bundle_build]{app_bundle_build()}} as \code{zone_names} — msens's own boot/manifest
builders stay \code{con}-only introspection and never open a GIS file mid-build.
}
\concept{app}
