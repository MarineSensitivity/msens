% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_cell_tile_check}
\alias{app_cell_tile_check}
\title{Assert every tile's cell ids satisfy the tile key at a given grid width}
\usage{
app_cell_tile_check(dir, ncol, con = NULL)
}
\arguments{
\item{dir}{the \code{app/cell} directory written by \code{\link[=app_cell_tiles]{app_cell_tiles()}}}

\item{ncol}{grid width to check against (7200 \code{global05}, 3103 \code{usa05})}

\item{con}{optional DuckDB connection to run the scan on}
}
\value{
\code{TRUE}, invisibly; errors on mismatch
}
\description{
The check that \strong{cannot pass on a mismatch}: it recomputes the tile id from each
\code{cell_id} and compares it with the partition the row is stored in. Counting rows
or listing files cannot tell a right width from a wrong one, because a wrong tile
id is still a valid tile id.
}
\concept{app}
