% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zone_set.R
\name{zone_key_col}
\alias{zone_key_col}
\title{The key column for a zone type}
\usage{
zone_key_col(zone_type, nms)
}
\arguments{
\item{zone_type}{one of \code{\link[=zone_set_key]{zone_set_key()}}'s types}

\item{nms}{column names of the layer}
}
\value{
the matching column name
}
\description{
A zone gpkg typically carries SEVERAL \verb{*_key} columns — the program-area
layer has \code{region_key}, \code{planarea_key} and \code{programarea_key} — so "the first
column ending in key" picks the wrong one. That is not a cosmetic error:
taking \code{region_key} (3 values) for the program-area layer yields 3 zones
instead of 20, and, because the ordering it induces is not total, leaves ties
whose order depends on the file's feature order — so two byte-identical
layers saved in different order could fingerprint differently.
}
\concept{zone_set}
