% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{zone_crosswalk}
\alias{zone_crosswalk}
\title{Crosswalk two zone layers by geometric identity}
\usage{
zone_crosswalk(a, b, key_a, key_b, iou_min = 0.999, hint = NULL)
}
\arguments{
\item{a, b}{\code{sf} polygon layers}

\item{key_a, key_b}{name of the zone-key column in each}

\item{iou_min}{minimum intersection/union to call two zones identical (default
\code{0.999})}

\item{hint}{optional column of \code{b} listing (comma-separated) \code{key_a} values to
test against; a hint with more than one key is tested as their union}
}
\value{
a data frame of every candidate pair: \code{key_a}, \code{key_b}, \code{iou},
\code{identical} (logical); the compared set is \code{identical == TRUE}
}
\description{
Pairs a zone of \code{a} with a zone of \code{b} only when the two polygons are the SAME
polygon — intersection over union at least \code{iou_min} — so two releases scored on
differently named units (v1's 36 Planning Areas, v2+'s 20 Program Areas) can be
compared where, and only where, they measured the same place. Measured on the
2025 Planning Areas vs the 2026 Program Areas: 18 of 20 Program Areas are
identical to a Planning Area (IoU 1.0000); the two Gulf of America Program
Areas are subsets (IoU 0.59, 0.15) and must NOT be compared, and 15 Planning
Areas (Atlantic, Hawai'i, Pacific islands) have no Program Area at all.
}
\details{
Candidate pairs come from \code{hint} when given (a column of \code{b} naming zones of
\code{a}, e.g. \code{programarea} rows carry \code{planarea_key = "CGA,EGA"}), else every pair
that intersects; either way the geometry decides. Planar areas in the layer's
CRS (\code{sf::sf_use_s2(FALSE)} while computing).
}
\concept{validate}
