% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_pmtiles}
\alias{publish_pmtiles}
\title{Publish vector features as PMTiles (tippecanoe)}
\usage{
publish_pmtiles(
  x,
  out_pmtiles,
  layer,
  minzoom = 0,
  maxzoom = 10,
  simplification = 10,
  keep_attrs = c("mdl_key", "ds_key"),
  tippecanoe = "tippecanoe",
  extra = character(0),
  quiet = TRUE
)
}
\arguments{
\item{x}{an sf object, or a path to a vector file tippecanoe can read
(FlatGeobuf / GeoJSON)}

\item{out_pmtiles}{output path (\code{.pmtiles})}

\item{layer}{tile layer name (the \code{source_layer} the app references)}

\item{minzoom, maxzoom}{zoom range (default 0..10). Simplification is applied
at the LOW zooms only, so maxzoom carries full source resolution and every
view above it overzooms from something faithful.}

\item{simplification}{tippecanoe \code{--simplification} for the low zooms only}

\item{keep_attrs}{attributes to carry into the tiles (\code{-y}); the zone layers
need their own key columns, not \code{mdl_key}/\code{ds_key}}

\item{tippecanoe}{path to the tippecanoe binary (default \code{"tippecanoe"})}

\item{extra}{extra tippecanoe CLI args (character vector)}

\item{quiet}{suppress tippecanoe stderr (default \code{TRUE})}
}
\value{
\code{out_pmtiles} (invisibly)
}
\description{
Writes an sf (or an existing vector file) to PMTiles via tippecanoe, keeping
every feature (no dropping) so a source dataset's ranges can be filtered
client-side by an attribute (e.g. \code{mdl_key}). Reprojects to EPSG:4326 first.
}
\concept{publish}
