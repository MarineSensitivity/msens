% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cog_store.R
\name{content_hash_sql}
\alias{content_hash_sql}
\title{Grouped content-fingerprint SQL}
\usage{
content_hash_sql(from, by, cols = c("cell_id", "val"))
}
\arguments{
\item{from}{table name or \code{read_parquet(...)} expression}

\item{by}{grouping column — the per-release model id (\code{mdl_seq} or \code{mdl_id})}

\item{cols}{payload columns to hash, in a FIXED order}
}
\value{
a SQL string yielding \code{by}, \code{n}, \code{x}, \code{s}
}
\description{
One pass, no sort and no spill — the whole point, since these tables run to
1.2 billion rows (measured: 325M rows in 7.8 s, so a full release hashes in
under a minute).
}
\concept{cog_store}
