% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{cells_in_polygon_grid}
\alias{cells_in_polygon_grid}
\title{Cells a polygon covers, from the grid definition alone}
\usage{
cells_in_polygon_grid(poly, grid)
}
\arguments{
\item{poly}{an \code{sf}, \code{sfc} or \code{sfg} polygon / multipolygon (EPSG:4326, or
transformable to it)}

\item{grid}{a \code{grid_id} (\code{"usa05"}, \code{"global05"}) or a spec list from
\code{\link[=grid_spec_for]{grid_spec_for()}}}
}
\value{
a tibble with \code{cell_id} (integer) and \code{pct_covered} (numeric 1-100),
ordered by \code{cell_id}
}
\description{
Returns \code{cell_id} + \code{pct_covered} (0-100) by pure arithmetic on a grid spec: the
polygon is intersected with the 0.05-degree squares the grid defines, planar in
degrees, and \code{pct_covered} is \code{round(area / (resx * resy) * 100)} with cells
rounding to 0 dropped. No \code{cell} table is read and no raster is opened, so the
answer depends on nothing but the polygon and the six numbers in the grid
registry -- which is what lets a browser reproduce it.
}
\details{
\strong{Coordinates are read LITERALLY} (master-plan D8 addendum, 2026-09-21). This
function guesses nothing about the antimeridian: longitudes must arrive
\strong{unwrapped}, so a Bering box is \verb{179.9 ... 180.1}, and a ring written wrapped
(\code{179.9} then \code{-179.9}) means, read literally, the 359.8-degree complement --
which is what it will return. Unwrapping is \code{\link[=unwrap_ring]{unwrap_ring()}}, one explicit rule
with a TypeScript twin and its own \verb{normalize-*} fixtures, applied at the input
boundary before coverage is ever computed.

What the grid does do is arithmetic its own definition requires: on \code{global05}
(7200 x 0.05 = 360, a grid that closes in longitude) an out-of-range column folds
back modulo \code{nc}, so \code{180.1} lands in column 1; on \code{usa05} (a 155.15-degree window
from 141.10 E) each vertex is shifted into that frame, because -90 simply IS 270
there, and a column still outside \verb{1..nc} is DROPPED, since there is no cell to
fold onto.

\strong{This is the definition, not an approximation of one.} \code{\link[=cells_in_polygon]{cells_in_polygon()}}
delegates to it for every database connection, so the drawn-polygon cell set, the
Program-Area cell set and the browser's cell set are one set.
}
\examples{
# a rectangle snapped to cell edges: whole cells only
p <- sf::st_sfc(sf::st_polygon(list(cbind(
  c(-90, -89.9, -89.9, -90, -90), c(27, 27, 27.1, 27.1, 27)))), crs = 4326)
cells_in_polygon_grid(p, "global05")
}
\concept{place}
