% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz.R
\name{add_cell_tiles}
\alias{add_cell_tiles}
\title{Add an msens cell tile layer to a mapgl map}
\usage{
add_cell_tiles(
  m,
  tile_url,
  id = "r_lyr",
  source_id = "r_src",
  tile_size = 256,
  raster_opacity = 0.8,
  raster_resampling = "nearest",
  before_id = NULL,
  ...
)
}
\arguments{
\item{m}{map or map_proxy}

\item{tile_url}{character(1) XYZ tile URL template, typically from
\code{\link[=cell_tile_url]{cell_tile_url()}} (must contain \code{{z}/{x}/{y}} placeholders)}

\item{id}{layer id (default: "r_lyr")}

\item{source_id}{source id (default: "r_src")}

\item{tile_size}{numeric (default: 256)}

\item{raster_opacity}{numeric (default: 0.8)}

\item{raster_resampling}{character (default: "nearest")}

\item{before_id}{layer to insert before}

\item{...}{additional args to \code{\link[mapgl:add_raster_layer]{mapgl::add_raster_layer()}}}
}
\value{
map (pipeable)
}
\description{
Viewport-driven XYZ tile alternative to \code{\link[=add_cells]{add_cells()}}: the browser only
fetches the tiles visible in the current viewport from the msens TiTiler
factory, rather than shipping the whole raster as a base64-encoded image.
}
\concept{viz}
