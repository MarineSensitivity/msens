% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_bundle_build}
\alias{app_bundle_build}
\title{Build the whole \verb{\{ver\}/app/} bundle into a directory}
\usage{
app_bundle_build(
  con,
  ver,
  dir_out,
  manifest = NULL,
  base = atlas_base_url(),
  geom_keys = list(),
  cell_tiles = TRUE,
  strict = TRUE,
  taxonomy_csv = NULL,
  built_at = format(Sys.time(), "\%Y-\%m-\%dT\%H:\%M:\%SZ", tz = "UTC")
)
}
\arguments{
\item{con}{a DBI connection to the release database}

\item{ver}{version label}

\item{dir_out}{output directory (created)}

\item{manifest}{a manifest from \code{\link[=manifest_build]{manifest_build()}}; built from \code{con} when \code{NULL}}

\item{base}{atlas base URL}

\item{geom_keys}{optional geometry keys per zone type, see \code{\link[=app_units]{app_units()}}}

\item{cell_tiles}{write the wide cell tiles (default \code{TRUE})}

\item{strict}{stop at the first failing stage (\code{TRUE}, the default), so a
half-written bundle is never published by accident. \code{FALSE} runs every stage,
records what failed in \verb{$failed}, and leaves the stages that worked on disk —
the notebook's diagnostic pass. Either way a failure NAMES its stage and the
outputs already written, so nobody needs to build a parallel composition to
get at the parts.}

\item{taxonomy_csv}{path to the WoRMS hierarchy CSV, passed to \code{\link[=app_taxonomy]{app_taxonomy()}}.
\code{NULL} writes no \code{taxonomy.parquet} and advertises none.}

\item{built_at}{ISO timestamp}
}
\value{
a list describing everything written, invisibly
}
\description{
Composes every builder, validates each output against its schema, and writes the
bytes. Nothing is uploaded here: publishing is the notebook's job and happens
only under \code{APP_BUNDLE_S3=1}.
}
\concept{app}
