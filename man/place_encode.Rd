% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{place_encode}
\alias{place_encode}
\alias{place_decode}
\title{Encode places into a URL hash, and decode them back (\code{g1})}
\usage{
place_encode(x)

place_decode(hash)
}
\arguments{
\item{x}{a place, or a list of places. A place is a list with \code{kind}:
\code{"geom"} (\code{name}, \code{geometry}, optional \code{precision}), \code{"zone"} (\code{set}, \code{keys}) or
\code{"upload"} (\code{name}, \code{digest}).}

\item{hash}{the hash value, with or without a leading \verb{#}}
}
\value{
\code{place_encode()} a hash string; \code{place_decode()} a list of places, each
with \code{kind} and, for \code{"geom"}, \code{geometry} as an \code{sfc} plus \code{precision}
}
\description{
A link is an analysis input: master-plan D8 puts places in the hash with a
versioned binary codec, and \strong{every analysis runs on the decoded geometry}, so a
link reproduces its numbers rather than approximating them. R needs to read those
links, which is why this is the twin of the app's \code{placeCodec.ts} rather than a
second design.
}
\section{The format}{

\verb{#pl = place ("~" place)*}, each place one of
\describe{
\item{\code{g1.name.b64url(bytes)}}{a drawn or uploaded geometry}
\item{\verb{z.set.key,key}}{zone keys, \code{set} in \code{pa|pl|er|sr} — the vintage comes
from the RELEASE, never from the token}
\item{\code{u.name.sha256_8}}{an upload too large to carry, naming what to re-upload}
}
\code{name} is percent-encoded UTF-8 over \verb{A-Za-z0-9_-} (so \code{.}, \code{~}, \verb{,} and \verb{\%}, all
structural here, cannot survive raw), at most 60 encoded characters.

The bytes are \code{0x10}, \code{precision:u8}, \code{npoly:varint}, then per polygon
\code{nring:varint} and per ring \code{npt:varint} followed by its vertices as
zigzag-varint \code{dlon}, \code{dlat} in units of \code{10^-precision}. The closing vertex is
omitted. \strong{The delta cursor starts at (0,0) and runs ACROSS rings and polygons} —
resetting it per ring is a bug that still decodes, into a different place.
Longitudes are stored \strong{unwrapped} (a Bering polygon runs 170..190), so a decoder
needs no antimeridian guess; \code{\link[=unwrap_ring]{unwrap_ring()}} is what puts them that way, and
\code{place_encode()} applies it.
}

\section{Precision}{

3 by default, 4 when the bounding box is under 0.5 degrees, so the deviation is at
most \code{0.5 * 10^-precision} — about 55 m at precision 3 and 5.5 m at precision 4.
}

\examples{
p <- list(kind = "geom", name = "Gulf box", geometry = sf::st_sfc(sf::st_polygon(
  list(cbind(c(-90, -89, -89, -90, -90), c(27.5, 27.5, 28.5, 28.5, 27.5)))),
  crs = 4326))
(h <- place_encode(p))
place_decode(h)[[1]]$name
}
\concept{app}
