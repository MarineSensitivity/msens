% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ver_token.R
\name{product_urls}
\alias{product_urls}
\title{Where this release's sibling products live}
\usage{
product_urls(
  ver,
  access = "public",
  app_base = "https://app.marinesensitivity.org",
  docs_base = "https://marinesensitivity.org/docs",
  home = "https://marinesensitivity.org"
)
}
\arguments{
\item{ver}{version label, e.g. \code{v8}}

\item{access}{\code{public} or \code{restricted} — from \code{\link[=atlas_ver_access]{atlas_ver_access()}}}

\item{app_base}{public app host}

\item{docs_base}{public docs base (GitHub Pages)}

\item{home}{project landing page}
}
\value{
named character vector: \code{scores}, \code{species}, \code{docs}, \code{home}
}
\description{
The MST is four things a reader moves between — two apps, the book, and the
project home — and each has been a dead end from the others: the scores table
links out to a species map with no way back, and the book's app links sit only
on its Preface (apps#11, docs#6). This is the one definition of that link set,
so a nav added to any of them cannot disagree with the rest.
}
\details{
\strong{Version-preserving is the point.} A bare link to \verb{/scores} from a v7 session
silently moves the reader to the promoted release, which is precisely the
confusion these navs exist to remove — so \verb{?ver=} is always carried. On a
\code{restricted} release the version is the URL PATH on the signed-in preview host
instead (\code{\link[=preview_app_url]{preview_app_url()}}), because Cloudflare Access scopes its per-version
policies by path; sending such a reviewer to \verb{?ver=} on the public host would
show them nothing.
}
\examples{
product_urls("v7")
product_urls("v9", access = "restricted")
}
\concept{version}
