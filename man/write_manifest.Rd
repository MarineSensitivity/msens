% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{write_manifest}
\alias{write_manifest}
\title{Write a deterministic, content-addressed target manifest}
\usage{
write_manifest(path, target, content_hash, stats = list(), force = FALSE)
}
\arguments{
\item{path}{manifest path (the notebook's \verb{output:})}

\item{target}{target name}

\item{content_hash}{from \code{\link[=hash_parquet]{hash_parquet()}} / \code{\link[=hash_query]{hash_query()}}}

\item{stats}{named list of deterministic summary stats}

\item{force}{rewrite even if unchanged (default \code{FALSE}; see \code{\link[=force_target]{force_target()}})}
}
\value{
\code{content_hash}, invisibly
}
\description{
The tracked \verb{output:} for a pipeline notebook. Bytes are a pure function of
\code{target}, \code{content_hash} and the (content-derived) \code{stats}, keys in fixed order,
NO wall-clock — so \code{targets} \code{format = "file"} re-hashes to the SAME value when
the data is unchanged and downstream targets do not re-run. Idempotent: if the
file already holds identical bytes it is left untouched (mtime preserved) unless
\code{force}. Keep \code{stats} deterministic (counts, ranges, versions) and free of
machine-specific paths so the manifest is host-independent.
}
\concept{manifest}
