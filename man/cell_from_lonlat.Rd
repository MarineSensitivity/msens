% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{cell_from_lonlat}
\alias{cell_from_lonlat}
\title{Cell id at a longitude/latitude (inverse of \code{\link[=cell_lonlat]{cell_lonlat()}})}
\usage{
cell_from_lonlat(lon, lat, grid)
}
\arguments{
\item{lon, lat}{coordinates in degrees, \code{lon} in -180..180}

\item{grid}{grid spec from \code{\link[=grid_spec_for]{grid_spec_for()}}}
}
\value{
cell id, or \code{NA} where the point falls outside the grid
}
\description{
Pure arithmetic on the grid definition -- no raster read. The cell-id COGs are
lookup IMAGES whose band name and frame vary by grid, so resolving a click by
reading one is both slower and a source of bugs: selecting the band by name
fails (usa05 calls it \code{r_cellid}, global05 \code{depth_mean}), and shifting a click
to 0-360 lands outside a raster stored in -180..180. The grid registry already
defines the mapping exactly.
}
\concept{grid}
