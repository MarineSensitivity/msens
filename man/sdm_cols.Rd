% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc.R
\name{sdm_cols}
\alias{sdm_cols}
\title{Resolve a release's column names for the cross-version queries}
\usage{
sdm_cols(con, mc_tbl = "model_cell")
}
\arguments{
\item{con}{open connection to a release database, or to a set of views over one}

\item{mc_tbl}{name of the model-cell table to inspect, or \code{NULL} to skip it.
Skipping matters on the server: inspecting \code{model_cell} there makes DuckDB LIST
the S3 prefix just to read a schema, which fails outright.}
}
\value{
a list with \code{valid}, \code{marine}, \code{tkey}, \code{mkey}, \code{val}
}
\description{
The v8 rewrite renamed three things every taxon/model query depends on, and the
app-side copy of one such query was never migrated — which is why the v8
"Table of Species" tab came up empty with \emph{"Can't select columns that don't
exist"}:
}
\details{
\tabular{lll}{
   concept \tab v1-v7 \tab v8 \cr
   taxon validity \tab \code{is_ok} \tab \code{is_valid_usa} \cr
   model id \tab \code{mdl_seq} \tab \code{ms_merge_key} (taxon) / \code{mdl_key} (\code{model_cell}) \cr
   cell value \tab \code{value} \tab \code{val} (\code{value} is reserved in DuckDB) \cr
}


Resolved \strong{per connection} by introspection, so one implementation serves every
release and no caller needs to know which generation it is talking to. Exported
because the versioned documentation asks the same question of the same published
tables — a second copy of the rule in the docs is exactly how a v3 page ends up
printing a v8 column name.

Note the semantics the names hide: v7's \code{is_ok} already baked in the
marine/category cull, while v8's \code{is_valid_usa} only means "has >= 1 merged cell
in US waters", so scoring eligibility on v8 additionally needs \code{is_marine}
(returned as \code{marine}, \code{NA} when the release has no such column).
}
\examples{
\dontrun{
con <- attach_atlas(version = "v7")
sdm_cols(con, mc_tbl = NULL)$valid   # "is_ok"
}
}
\concept{calc}
