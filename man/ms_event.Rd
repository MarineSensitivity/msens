% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{ms_event}
\alias{ms_event}
\title{Build a tracking-event payload}
\usage{
ms_event(event, ...)
}
\arguments{
\item{event}{event name, e.g. \code{"select_species"}; coerced to GA4-safe form}

\item{...}{named event parameters (scalars); \code{NULL}/\code{NA}/\code{""} are dropped}
}
\value{
list with \code{event} (character scalar) and \code{params} (named list)
}
\description{
The pure payload constructor shared by \code{\link[=ms_track]{ms_track()}} and the tests. Normalises
the event name to GA4's rules (lowercase, \verb{[a-z0-9_]}, leading letter,
truncated to 40 characters) and drops \code{NULL}/\code{NA}/empty parameters so the
Sheet's \code{params} column stays readable.
}
\details{
Parameter \emph{values} are NOT truncated here — the Sheet leg wants the full
string (a long search term, a full scientific name). The client truncates to
100 characters for the gtag leg only.
}
\examples{
ms_event("Select Species", scientific_name = "Dermochelys coriacea", n = 3)
ms_event("download", file = NULL, format = "csv")   # NULL dropped
}
\concept{analytics}
