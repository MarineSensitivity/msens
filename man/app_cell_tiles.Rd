% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_cell_tiles}
\alias{app_cell_tiles}
\title{Write the wide \verb{app/cell/tile=\{t\}/} Parquet tiles}
\usage{
app_cell_tiles(con, dir_out, ncol = cell_grid_ncol(con))
}
\arguments{
\item{con}{a DBI connection}

\item{dir_out}{directory to write \code{tile={t}/data_0.parquet} into}

\item{ncol}{grid width; defaults to the release's own \code{\link[=cell_grid_ncol]{cell_grid_ncol()}}}
}
\value{
a list: \code{dir}, \code{tiles}, \code{rows}, \code{metric_keys}, \code{ncol}
}
\description{
One DOUBLE column per scored \code{metric_key}, so nothing joins on \code{metric_seq} —
which \code{score_cell_metrics.qmd} drops and recreates every run.
}
\details{
\strong{Which rows.} Every \code{cell} row of any tile that holds a \code{cell_metric} or
\code{cell_model} row, so the browser's \verb{JOIN cell c USING (cell_id)} keeps exactly the
rows R's does. \code{in_usa} / \code{in_pra} are NULL where a release has no such column.

The tile key is \code{\link[=cell_model_tile_sql]{cell_model_tile_sql()}} with the RELEASE'S OWN grid width. A wrong
width computes a different, perfectly valid tile id, \verb{WHERE tile IN (…)} prunes
away the only partition holding the cells, and the query returns nothing at all —
see \code{\link[=cell_model_tile_check]{cell_model_tile_check()}} and \code{\link[=app_cell_tile_check]{app_cell_tile_check()}}.
}
\concept{app}
