% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_zones_unique}
\alias{app_zones_unique}
\title{Assert every zone-keyed object holds each zone exactly once}
\usage{
app_zones_unique(zt, boot)
}
\arguments{
\item{zt}{the zone_taxon frame from \code{\link[=app_zone_taxon]{app_zone_taxon()}}}

\item{boot}{the boot object}
}
\value{
\code{TRUE}, invisibly; errors naming the first duplicated keys
}
\description{
A hard stop, not a warning. Two \code{zone.tbl} for one \code{fld} (v2's \code{subregion_key})
silently duplicated 13,077 \verb{(zone_fld, zone_value, key)} rows in
\code{zone_taxon.parquet} and gave \code{boot$zones$subregion} six rows for four subregions.
Nothing errored; the app would simply have shown every taxon twice.
}
\concept{app}
