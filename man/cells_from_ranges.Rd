% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{cells_from_ranges}
\alias{cells_from_ranges}
\title{Rasterize range polygons onto the global 0.05° cell grid (whole range)}
\usage{
cells_from_ranges(
  x,
  cellid_tif,
  value = 100,
  cover = FALSE,
  min_coverage = 0,
  max_vertices = 1000,
  exact = FALSE
)
}
\arguments{
\item{x}{an \code{sf}/\code{SpatVector} of (multi)polygons in EPSG:4326 \link{-180,180}}

\item{cellid_tif}{path to the GLOBAL cell-id COG (\code{cell_id} for every cell)}

\item{value}{value for a covered cell. For range datasets this is the species'
\strong{extinction-risk score from \code{\link[=compute_er_score]{compute_er_score()}}} (e.g.
\code{compute_er_score("NMFS:EN")} = 100, \code{"NMFS:TN"} = 50, \code{"IUCN:CR"} = 50) —
NEVER a hard-coded number. Default 100 is only a convenience.}

\item{cover}{logical; \code{TRUE} = coverage fraction × \code{value}, \code{FALSE} (default) =
flat \code{value} for any overlap}

\item{min_coverage}{keep cells whose covered fraction exceeds this (default 0 =
any overlap; use 0.5 for a majority/centre-like rule)}

\item{max_vertices}{(exact path only) subdivide the range into chunks of at
most this many vertices before rasterizing (default 1000). A globe-spanning
range (a wide-ranging seabird) is one massive multipolygon that hangs
\code{st_union} / \code{exact_extract}; subdividing bounds each chunk's raster window so
it never touches the whole-globe polygon. Cells are de-duplicated afterward,
so chunking is equivalent to a union here. Needs the \code{lwgeom} package; without
it, falls back to the single-union path.}

\item{exact}{force the coverage-aware \code{exact_extract} path even for the flat
any-overlap case (default \code{FALSE}). By default the flat case
(\code{cover=FALSE}, \code{min_coverage=0}) uses the ~2x-faster \code{terra::rasterize}
touches path, which yields an identical cell set; set \code{TRUE} for the
boundary-exact coverage>0 behavior. \code{cover=TRUE} or \code{min_coverage>0} always
use the exact path.}
}
\value{
a tibble \verb{(cell_id integer, val double)}; empty if no overlap
}
\description{
Turns presence polygons (an IUCN/BOTW/NMFS range, a critical-habitat polygon)
into \verb{(cell_id, value)} on the v8 grid, capturing the \strong{whole} distribution —
land AND ocean (v8 does not mask land). Uses \code{exactextractr}, which is fast and
robust to messy geometry (self-intersections, MULTISURFACE — the usual range-map
"funk"). By default every cell the polygon overlaps gets \code{value} (100 =
presence); \code{cover = TRUE} weights by the fraction of each cell covered. Marine
share is separate — see \code{\link[=cells_pct_marine]{cells_pct_marine()}}.
}
\examples{
\dontrun{
r <- msens::cells_from_ranges(ply_sp, cellid_tif)               # presence = 100, whole range
r <- msens::cells_from_ranges(ply_sp, cellid_tif, cover = TRUE) # coverage-weighted
}
}
\concept{ingest}
