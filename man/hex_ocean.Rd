% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hex.R
\name{hex_ocean}
\alias{hex_ocean}
\title{Enumerate the ocean H3 hex tiling from a raster mask (GEOMETRY ONLY)}
\usage{
hex_ocean(r, con, res = HEX_RES, mask_layer = names(r)[1], out_tbl = "hex")
}
\arguments{
\item{r}{a \link[terra:SpatRaster-class]{terra::SpatRaster} in EPSG:4326}

\item{con}{a DuckDB connection}

\item{res}{H3 resolution (default \link{HEX_RES})}

\item{mask_layer}{layer whose non-NA pixels define ocean coverage
(default \code{names(r)[1]})}

\item{out_tbl}{DuckDB table to (over)write with columns \code{hex_id} (BIGINT) and
\code{area_km2} (default \code{"hex"})}
}
\value{
invisibly, \code{out_tbl}
}
\description{
Defines \emph{which} res-\code{res} hexagons tile the ocean and their \code{area_km2} — it
does \strong{not} interpolate any values (see the module INTERPOLATION PRINCIPLE).
A res-7 hex (~5.16 km^2) is smaller than a 0.05 degree pixel (~23 km^2), so
each ocean pixel (non-NA in \code{mask_layer}) is polyfilled into the hexes whose
\emph{centre} falls inside it (\code{h3_polygon_wkt_to_cells}, center-containment → a
gap-free, overlap-free tiling). Values are attached afterwards with
\code{\link[=hex_interp_idw]{hex_interp_idw()}} from the source centroids.
}
\concept{hex}
