% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc.R
\name{sdm_val_col}
\alias{sdm_val_col}
\title{Name of the measurement column in a release's table}
\usage{
sdm_val_col(con, tbl)
}
\arguments{
\item{con}{open connection to a release database, or to a set of views over one}

\item{tbl}{name of the table to inspect, e.g. \code{"zone"}, \code{"cell_metric"}}
}
\value{
\code{"val"} or \code{"value"}
}
\description{
Returns \code{"val"} or \code{"value"} — whichever the given table actually has.
}
\details{
Releases disagree, and \strong{so do the two forms of the same release}. v1-v7 named the
measurement \code{value} throughout (\code{zone}, \code{zone_metric}, \code{cell_metric}, \code{model_cell}).
v8 renamed it \code{val}, away from DuckDB's reserved word — but the release step \emph{also}
writes a \code{value} alias into the served views, so a v8 \code{serve.duckdb} carries \strong{both}
while the v8 source \code{sdm.duckdb} it was built from carries only \code{val}.

The practical consequence, and the reason this is a function rather than a constant:
code that hardcodes \code{value} runs against every served release and fails against a v8
source database, while code that hardcodes \code{val} does the exact opposite. Both spellings
appeared in one app file. \code{val} is preferred where both exist, since that is the name
the data is actually stored under.

The failure this prevents is not a clean missing-column error. In dplyr/dbplyr a bare
\code{value} with no such column resolves to a \emph{function} further up the scope chain, so the
error reads \verb{cannot coerce type 'closure' to vector of type 'character'} from somewhere
inside the SQL translator, far from the table that lacks the column.
}
\examples{
\dontrun{
con <- attach_atlas(version = "v7")
sdm_val_col(con, "zone_metric")   # "value"
}
}
\concept{calc}
