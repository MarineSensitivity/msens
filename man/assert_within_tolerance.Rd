% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{assert_within_tolerance}
\alias{assert_within_tolerance}
\title{Assert a delta table is within tolerance, else error (pure)}
\usage{
assert_within_tolerance(d, mean_tol, max_tol, delta_col = "delta")
}
\arguments{
\item{d}{a tibble from \code{\link[=score_delta]{score_delta()}}}

\item{mean_tol}{tolerance on mean absolute delta}

\item{max_tol}{tolerance on max absolute delta}

\item{delta_col}{name of the delta column (default \code{"delta"})}
}
\value{
(invisibly) the \code{\link[=score_delta_summary]{score_delta_summary()}} list
}
\description{
The equivalence gate: fails (stops) if \verb{mean|Δ|} exceeds \code{mean_tol} or
\verb{max|Δ|} exceeds \code{max_tol}. Returns the summary invisibly on success so a
notebook can report it.
}
\concept{validate}
