% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hex.R
\name{hex_interp_idw}
\alias{hex_interp_idw}
\title{Interpolate source values onto hex centroids (inverse-distance weighting)}
\usage{
hex_interp_idw(
  con,
  src,
  hex_tbl,
  val_cols,
  k = 8L,
  power = 2,
  lon = "lon",
  lat = "lat",
  chunk = 5000000L
)
}
\arguments{
\item{con}{a DuckDB connection (opened \code{bigint = "integer64"})}

\item{src}{a data.frame of source points with \code{lon}, \code{lat}, and \code{val_cols}}

\item{hex_tbl}{name of the DuckDB hex table (must have \code{hex_id} BIGINT)}

\item{val_cols}{character vector of value columns in \code{src} to interpolate}

\item{k}{number of nearest source points (default 8)}

\item{power}{IDW power (default 2)}

\item{lon, lat}{coordinate column names in \code{src} (default "lon","lat")}

\item{chunk}{hex rows processed per kd-tree query batch (default 5e6)}
}
\value{
invisibly, \code{hex_tbl}
}
\description{
The canonical hex interpolation: for every hexagon in \code{hex_tbl}, take the \code{k}
nearest source points (by great-circle distance, via a unit-sphere kd-tree so
it is correct at the poles and antimeridian) and compute the inverse-distance
weighted mean (\code{weight = 1 / d^power}) of each \code{val_cols} column, skipping NA
source values per column. The interpolated columns are appended to \code{hex_tbl}.
For a regular-grid source this reproduces a bilinear-style interpolation; for
scattered sources it is classic IDW. This is the ONLY sanctioned way to move
values onto hexes — never inherit a containing pixel's value.
}
\concept{hex}
