% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{stac_catalog_register}
\alias{stac_catalog_register}
\title{Register a version in a deployed root catalog (idempotent)}
\usage{
stac_catalog_register(catalog_json, version, cfg = stac_cfg(version))
}
\arguments{
\item{catalog_json}{path to the deployed root \code{catalog.json}}

\item{version}{version to register, e.g. \code{"v7b"}}

\item{cfg}{config from \code{\link[=stac_cfg]{stac_cfg()}} (used only when the catalog does not exist yet)}
}
\value{
invisibly, the child hrefs now in the catalog, in order
}
\description{
\code{\link[=stac_build]{stac_build()}} writes a root \code{catalog.json} that knows only the version it just built, so
copying it over a deployed catalog would orphan every other release. This edits the DEPLOYED
root instead: it adds \code{./{version}/collection.json} as a child if it is not already there and
keeps the children in release order (\code{v7}, \code{v7b}, \code{v8}, ... — numeric part, then suffix), which
is how a patch release dated after its successors still lands beside the release it patches.
A missing catalog is created.
}
\concept{stac}
