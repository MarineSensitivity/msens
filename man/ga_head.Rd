% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{ga_head}
\alias{ga_head}
\title{Analytics \verb{<head>} snippet as a Shiny tag}
\usage{
ga_head(
  app,
  content_group = app,
  app_version = "",
  ip = "",
  measurement_id = .MS_GA_ID,
  log_url = Sys.getenv("MSENS_LOG_URL", "")
)
}
\arguments{
\item{app}{short app/product id recorded on every event, e.g. \code{"scores"}}

\item{content_group}{GA4 content group for reporting; defaults to \code{app}}

\item{app_version}{version string recorded on every event — the deployed git
commit, so a Sheet row ties back to the exact code that produced it}

\item{ip}{client IP to stamp on every logged row, from
\code{ms_client_ip(req)} in a \verb{ui = function(req)}. Behind shiny-server this is
the ONLY place a real address exists — see \code{\link[=ms_client_ip]{ms_client_ip()}}.}

\item{measurement_id}{GA4 measurement ID; defaults to the project property}

\item{log_url}{Apps Script \verb{/exec} endpoint for the Sheet log. Defaults to the
\code{MSENS_LOG_URL} environment variable; empty means the Sheet leg is a silent
no-op (GA4 still receives events).}
}
\value{
an \code{html} object
}
\description{
\code{\link[=ga_js]{ga_js()}} wrapped in \code{\link[htmltools:HTML]{htmltools::HTML()}} for use inside \code{tags$head()}.
}
\examples{
\dontrun{
ui <- bslib::page_sidebar(
  tags$head(msens::ga_head("scores", app_version = "v8")), ...)
}
}
\concept{analytics}
