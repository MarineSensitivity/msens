% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_model.R
\name{cell_grid_ncol}
\alias{cell_grid_ncol}
\title{Grid width (columns) a database's \code{cell_model} was tiled with}
\usage{
cell_grid_ncol(con = NULL)
}
\arguments{
\item{con}{a DBI connection, or \code{NULL} for the default}
}
\value{
integer number of grid columns
}
\description{
The tile key depends on how many columns the grid has, and that differs by
version (v8 global: 7200; v7 regional: 3103). Readers must use the SAME width
the writer used or \verb{tile IN (…)} prunes away the very rows being sought — and
silently, since a wrong tile id is still a valid tile id.
}
\details{
Resolution order: the \code{cell_grid} table written alongside \code{cell_model}, then
the grid the connection itself implies (\code{\link[=grid_for_con]{grid_for_con()}}), then the compiled
default. v8/v9 resolve to 7200 either way; v1-v7 resolve to 3103 instead of
silently taking the global width.
}
\concept{calc}
