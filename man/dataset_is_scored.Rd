% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdl_key.R
\name{dataset_is_scored}
\alias{dataset_is_scored}
\title{Which datasets actually fed the scored surface?}
\usage{
dataset_is_scored(ds_key, taxon_model_ds_key, merged_key = "ms_merge")
}
\arguments{
\item{ds_key}{character vector of dataset keys to classify}

\item{taxon_model_ds_key}{\code{ds_key} column of that release's \code{taxon_model} (the
taxon ↔ contributing-model edges); \code{NULL} when the release does not publish the
relation, in which case every registered dataset is assumed scored — the honest
answer when the release gives no way to tell}

\item{merged_key}{the merged dataset's key}
}
\value{
logical vector parallel to \code{ds_key}
}
\description{
A release's \code{dataset} table registers every dataset that was \emph{ingested}, which is
not the same as every dataset the scores were computed from. v8 ingests NOAA SEFSC
(\code{gm}) and NCCOS (\code{nc}) density models into \verb{dist/}, but their density units
(#/km²) are not yet mapped onto the \link{0,100} suitability scale, so they are
deliberately excluded from the merge — and therefore contribute nothing to any
score. Listing them beside AquaMaps as though they did overstates the inputs.
}
\details{
The test is introspected, never asserted: a dataset is scored iff it contributed at
least one model to a merged taxon, i.e. its \code{ds_key} appears in \code{taxon_model}. The
merged dataset itself (\code{ms_merge}) is scored by construction — it \emph{is} the surface
scoring reads.
}
\examples{
dataset_is_scored(c("ms_merge", "am", "gm"), c("am", "am", "bl"))  # TRUE TRUE FALSE
}
\concept{mdl_key}
