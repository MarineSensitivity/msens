% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_palettes}
\alias{app_palettes}
\title{The 11-stop palettes the app must draw with}
\usage{
app_palettes(names = c("spectral_r", "viridis", "cividis", "magma"))
}
\arguments{
\item{names}{colormap names to build (default: the four the contract lists)}
}
\value{
a named list of 11 upper-case \verb{#RRGGBB} strings each
}
\description{
The legend, the popup swatch and the choropleth bins have to equal what R drew
and what titiler rendered, so the ramps are shipped rather than re-derived by a
JavaScript colour library interpolating in a different space.
}
\details{
\code{spectral_r} is ColorBrewer Spectral reversed, interpolated in \strong{Lab}
(\code{colorRampPalette(space = "Lab")}), which is what \code{msens::viz} uses. The
perceptual ramps come from \code{viridisLite}, whose values are matplotlib's own — the
same table titiler renders from. \code{grDevices::hcl.colors()} is deliberately not
used: it re-derives them in HCL and, on some builds, errors outright on \code{Cividis}.

Unknown names are an error, never a silent substitution: a layer rendered in the
wrong ramp is a legend that lies.
}
\concept{app}
