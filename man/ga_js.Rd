% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{ga_js}
\alias{ga_js}
\title{Analytics \verb{<head>} snippet (GA4 + batched Sheet beacon)}
\usage{
ga_js(
  app,
  content_group = app,
  app_version = "",
  measurement_id = .MS_GA_ID,
  log_url = Sys.getenv("MSENS_LOG_URL", "")
)
}
\arguments{
\item{app}{short app/product id recorded on every event, e.g. \code{"scores"}}

\item{content_group}{GA4 content group for reporting; defaults to \code{app}}

\item{app_version}{version string recorded on every event, e.g. \code{"v8"}}

\item{measurement_id}{GA4 measurement ID; defaults to the project property}

\item{log_url}{Apps Script \verb{/exec} endpoint for the Sheet log. Defaults to the
\code{MSENS_LOG_URL} environment variable; empty means the Sheet leg is a silent
no-op (GA4 still receives events).}
}
\value{
character scalar of raw HTML (\verb{<script>} tags)
}
\description{
Generates the self-contained HTML/JS installed once per page: the GA4 gtag
loader, the client-side event queue that beacons to the usage-log Sheet, a
\code{Shiny.addCustomMessageHandler("msTrack", ...)} handler so \code{\link[=ms_track]{ms_track()}} works,
and a \code{window.msTrack(event, params)} helper for page JS.
}
\details{
The same snippet serves every product; \code{content_group} is what separates them
in GA4 reporting, so no product needs its own measurement ID.
}
\examples{
substr(ga_js(app = "scores", app_version = "v8"), 1, 60)
}
\concept{analytics}
