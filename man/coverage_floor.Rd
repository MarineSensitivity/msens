% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{coverage_floor}
\alias{coverage_floor}
\title{Flag which (zone, metric) pairs clear the coverage floor}
\usage{
coverage_floor(d, kappa = 0.05, tol = 1e-09)
}
\arguments{
\item{d}{data frame with a numeric \code{coverage} column (typically from \code{\link[=coverage_sql]{coverage_sql()}}).}

\item{kappa}{numeric in \verb{[0, 1]}; the floor, default \code{0.05} (5 \%).}

\item{tol}{numeric; absolute tolerance on the boundary comparison, default \code{1e-9}.}
}
\value{
\code{d} with a logical \code{reportable} column added.
}
\description{
Adds \code{reportable = coverage >= kappa} to the result of \code{\link[=coverage_sql]{coverage_sql()}}. A pair below the floor
is \strong{not reportable}: the caller deletes its zone-metric row, so it is excluded from the
composite exactly as a component with no cells is -- it is never published as a zero.
}
\details{
The boundary is inclusive and is compared with a tolerance, because the fraction arrives from a
floating-point division: 5 of 100 equally weighted cells must be reportable at \code{kappa = 0.05},
and whether \code{5/100} lands a half-ulp below \code{0.05} depends on the weights that produced it.
}
\examples{
coverage_floor(data.frame(zone_seq = 1:2, metric_key = "m", coverage = c(0.04, 0.05)))
}
\concept{calc}
