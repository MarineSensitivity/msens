% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_cog}
\alias{publish_cog}
\title{Publish one model's cells as a COG}
\usage{
publish_cog(
  cell_id,
  val,
  out_tif,
  grid,
  datatype = "INT1U",
  nodata = 0,
  overview = TRUE
)
}
\arguments{
\item{cell_id}{integer vector of global row-major cell ids (1-based)}

\item{val}{numeric vector of values aligned to \code{cell_id} (e.g. 1-100)}

\item{out_tif}{output path (\code{.tif})}

\item{grid}{grid spec from \code{\link[=grid_spec]{grid_spec()}}}

\item{datatype}{terra datatype (default \code{"INT1U"}; use \code{"FLT4S"} for floats)}

\item{nodata}{value flagged as NoData (default \code{0}; AquaMaps vals are 1-100)}

\item{overview}{logical; build internal overviews (default \code{TRUE})}
}
\value{
\code{out_tif} (invisibly), or \code{NULL} if no finite cells
}
\description{
Paints a model's \verb{(cell_id, val)} onto the global 0.05° grid, cropped to the
cells' bounding box, and writes a Cloud-Optimized GeoTIFF with internal
overviews (so a global range renders cheaply at low zoom via titiler \verb{/cog}).
Cropping to the data window keeps each COG small (most ranges are regional).
}
\concept{publish}
