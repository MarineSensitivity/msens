% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge.R
\name{dps_sql}
\alias{dps_sql}
\title{Merge rule for NMFS DPS-listed species: the model stays the distribution, ER rides beside it}
\usage{
dps_sql(dps_ds, suit_ds, src = "dps_src")
}
\arguments{
\item{dps_ds}{character vector; ds_key(s) of the per-cell extinction-risk dataset(s) (\code{"dps_nmfs"}).}

\item{suit_ds}{character vector; suitability ds_key(s) (\code{c("am", "ax")} from v9).}

\item{src}{character; name of the source relation \verb{(ms_merge_key, ds_key, cell_id, val)}.}
}
\value{
SQL string selecting \verb{(mdl_key, cell_id, val, er)} — the whole-range surface with its
per-cell extinction risk.
}
\description{
For species whose ESA listing is scoped below the species — Distinct Population Segments, ESUs,
subspecies (\code{dps_nmfs}: humpback whale, Southern Resident killer whale, the salmonid ESUs, …) —
extinction risk varies across the range, but unlike the sea turtles (\code{\link[=turtle_sql]{turtle_sql()}}) the risk is
NOT multiplied into the merged value. The merged model is the \strong{distribution}: the suitability
surface (\code{suit_ds}, max over the survivors of \code{\link[=supersede_sql]{supersede_sql()}}) masked to the ER footprint (the
species' IUCN range ∪ its listed entities' habitat, i.e. the same range mask every other taxon
gets), and where no suitability model covers a footprint cell the cell is valued at its ER — the
plain rule's "fitting point" (\code{greatest(er, coalesce(suit, 0))} reduces to \code{er} there). The
per-cell ER is returned \strong{beside} the value (\code{er}) for \code{score_cell_metrics} to multiply in,
exactly as the taxon-level \code{er_score} is for every other species: \verb{extrisk = er × val / 100}.
}
\details{
Why not the turtle rule: \verb{round(er × suit / 100)} with the humpback's ER of 1 across 99.8 \% of
its range collapsed the merged surface to 1 everywhere except critical habitat — the merged
model showed the \emph{weight}, not the whale. Critical-habitat datasets are not an input here (the
caller excludes \verb{ch_*} from \code{src}): \code{dps_nmfs} already carries every designation with its own
entity's status, while \verb{ch_*} carries the species-level status.
}
\examples{
cat(dps_sql("dps_nmfs", c("am", "ax")))
}
\concept{merge}
