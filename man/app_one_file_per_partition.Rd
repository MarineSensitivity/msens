% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_one_file_per_partition}
\alias{app_one_file_per_partition}
\title{Assert a partitioned bundle directory holds exactly one file per partition}
\usage{
app_one_file_per_partition(dir, name = basename(dir))
}
\arguments{
\item{dir}{a partitioned directory (\verb{tile=\{t\}/} beneath it)}

\item{name}{what to call it in the error}
}
\value{
the number of partitions, invisibly; errors on any partition with a file
count other than one
}
\description{
\strong{Anonymous LIST is denied on the bucket}, so a static client cannot discover
parts: it constructs \code{tile={t}/data_0.parquet} and reads whatever is there. A
second part is therefore not a performance detail, it is missing data that nobody
is told about — and a gate that globs \verb{**/*.parquet} reads all six parts and sees
nothing wrong. Every partitioned thing the bundle writes goes through this.
}
\concept{app}
