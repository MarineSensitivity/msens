% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{cells_from_aligned_raster}
\alias{cells_from_aligned_raster}
\title{\verb{(cell_id, val)} from a raster ALREADY on the cell grid (no resample)}
\usage{
cells_from_aligned_raster(
  x,
  cellid_tif,
  band = 1,
  scale = 1,
  min_value = 1,
  digits = 2,
  tol = 1e-04
)
}
\arguments{
\item{x}{a \code{SpatRaster} (one layer) or a path; \code{band} selects the layer from a path}

\item{cellid_tif}{path to the grid's cell-id COG, OR an integer vector of cell ids read
from it once (\code{terra::values(rast(cellid_tif), mat = FALSE)}) — pass the vector when
calling in a loop, so the 100 MB id raster is read once per worker, not once per model}

\item{band}{layer index when \code{x} is a path (default 1)}

\item{scale}{multiply source values by this (AquaX 0–1000 → \code{0.1} gives the \link{0,100} scale)}

\item{min_value}{drop scaled values below this (default 1, like AquaMaps)}

\item{digits}{rounding of \code{val} (default 2)}

\item{tol}{extent tolerance in degrees (default 1e-4; the cell-id COG carries ~6e-6 of
float drift against the nominal grid)}
}
\value{
a tibble \verb{(cell_id integer, val double)}
}
\description{
The v8+ \code{global05} grid was anchored to the shared 0.05° \link{-180,180} topology of Bio-Oracle
and AquaX so that such a raster maps to \code{cell_id} by \strong{position}: pixel \code{i} of the
source is pixel \code{i} of the cell-id COG. This reads the cell ids at the source's non-NA
pixels rather than assuming \code{cell_id == i} — on \code{global05} the two coincide (and the AquaX
ingest asserts it), while a lookup-image grid (\code{usa05}'s \code{r_cellid.tif}) carries ids that
are not positions. Land pixels (\code{NA} in the cell-id raster) are dropped.
}
\details{
No resample, no zero-fill, no land mask: the source's own values define coverage.
}
\concept{ingest}
