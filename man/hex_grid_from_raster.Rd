% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hex.R
\name{hex_grid_from_raster}
\alias{hex_grid_from_raster}
\title{Build an H3 hex grid from a raster by exploding pixels into child hexes}
\usage{
hex_grid_from_raster(
  r,
  con,
  res = HEX_RES,
  mask_layer = names(r)[1],
  out_tbl = NULL
)
}
\arguments{
\item{r}{a \link[terra:SpatRaster-class]{terra::SpatRaster} in EPSG:4326 (layer names are carried through
as the per-hex value columns)}

\item{con}{a DuckDB connection}

\item{res}{H3 resolution (default \link{HEX_RES})}

\item{mask_layer}{name of the layer whose non-NA pixels define coverage
(default \code{names(r)[1]})}

\item{out_tbl}{optional DuckDB table to (over)write with \code{hex_id} as BIGINT;
if \code{NULL}, a tibble is returned with \code{hex_id} as \code{bit64::integer64}}
}
\value{
the \code{out_tbl} name (invisibly) or a tibble
}
\description{
A res-7 hexagon (~5.16 km^2) is \emph{smaller} than a 0.05 degree ocean pixel
(~23 km^2 at 40N), so indexing each pixel centroid to a hex would under-sample
the grid (leaving gaps). Instead this explodes each non-NA pixel (per
\code{mask_layer}) into the res-\code{res} hexes whose \emph{center} falls inside the pixel
(DuckDB \code{h3_polygon_wkt_to_cells}), and each hex inherits that pixel's layer
values plus \code{area_km2} (\code{h3_cell_area}). Because polyfill uses center
containment, adjacent pixels partition the hexes with no overlap. Intended for
sources at or coarser than the hex resolution (Bio-Oracle, AquaMaps, NCCOS).
}
\concept{hex}
