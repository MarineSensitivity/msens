% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas.R
\name{atlas_views}
\alias{atlas_views}
\title{Create named views over the released atlas tables}
\usage{
atlas_views(
  con,
  base = attr(con, "atlas_base"),
  region = "us-east-1",
  anon = FALSE
)
}
\arguments{
\item{con}{connection from \code{\link[=attach_atlas]{attach_atlas()}}}

\item{base}{atlas base URL (defaults to \code{attr(con, "atlas_base")})}

\item{region}{S3 region}

\item{anon}{if \code{TRUE}, skip the credentialed \code{model_cell} glob view}
}
\value{
\code{con}, invisibly
}
\description{
Mirrors the view set the serving \code{serve.duckdb} exposes, so the calc/score
helpers that reference bare table names (\code{tbl(con, "zone")}, \code{scores_for_pra()},
\code{species_for_cells()}, …) compose directly with an \code{\link[=attach_atlas]{attach_atlas()}} connection.
Single-file tables are read via \strong{path-style HTTPS} (anonymous GET + HTTP range;
the dotted bucket breaks virtual-hosted TLS). \code{model_cell} is Hive-partitioned
under \verb{serve/} and its glob needs S3 LIST, so it is created only when
\code{anon = FALSE} (credentialed); it joins \code{model} back so ad-hoc queries select by
the stable \code{mdl_key}, not the volatile \code{mdl_id}. The scoring tables store the
metric in \code{val}; a \verb{val AS value} alias is exposed for back-compat.
}
