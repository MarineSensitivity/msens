% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_boot}
\alias{app_boot}
\title{Build \code{app/boot.json}}
\usage{
app_boot(
  con,
  ver,
  manifest,
  tables = list(),
  geom_keys = list(),
  zone_sets = NULL,
  chosen = NULL,
  zone_names = NULL,
  built_at = format(Sys.time(), "\%Y-\%m-\%dT\%H:\%M:\%SZ", tz = "UTC")
)
}
\arguments{
\item{con}{a DBI connection to a release database}

\item{ver}{version label}

\item{manifest}{a manifest from \code{\link[=manifest_build]{manifest_build()}}; \code{metrics} supplies the COGs
and \code{zones} the PMTiles}

\item{tables}{named list of \code{name -> list(href, bytes, digest)}; see
\code{\link[=app_table_manifest]{app_table_manifest()}}}

\item{geom_keys}{optional geometry keys per zone type, see \code{\link[=app_units]{app_units()}}}

\item{zone_sets}{the zone-set registry, used by \code{\link[=app_zone_tbl]{app_zone_tbl()}} to choose the one
table per field when a release carries more than one}

\item{chosen}{a precomputed \code{\link[=app_zone_tbl]{app_zone_tbl()}} result, so every builder in one
bundle agrees on the same table}

\item{zone_names}{optional \code{data.frame(fld, key, name)} from \code{\link[=app_zone_names]{app_zone_names()}},
rbound across every zone type the release publishes — human names for
\verb{zones.<unit>[i].name} (e.g. "Aleutian Arc" for \code{ALA}). \code{NULL} (default)
publishes every zone with \code{name = NULL}, exactly as before.}

\item{built_at}{ISO timestamp to stamp (default: now, UTC, second precision)}
}
\value{
a validated \code{boot.json} object
}
\description{
Everything the app needs for first paint with no WASM and no second request:
the release's identity and access, the grid, the study-area camera presets, the
drawable units and their keys, the cell layers with their COGs and rescales, every
zone's precomputed metrics, the versioned flower default, the datasets, the colour
ramps and the table manifest with a digest per table (the OPFS invalidation key).
}
\details{
\code{flower_default} is \strong{versioned here}, which retires the shared, unversioned
\code{scores/cache/flower_default_subregions.csv}: the first release to run wrote it
and every other release then read it, so the committed file holds 10 components
from a release that had 10 while v8/v9 have 8.
}
\concept{app}
