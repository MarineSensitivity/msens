% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage.R
\name{build_storage_index}
\alias{build_storage_index}
\title{Generate index pages for a bucket tree}
\usage{
build_storage_index(
  objs,
  site_url = "https://storage.marinesensitivity.org",
  obj_url = "https://s3.us-east-1.amazonaws.com/oceanmetrics.io-public",
  max_child_dirs = 500L,
  max_rows = 2000L
)
}
\arguments{
\item{objs}{data frame from \code{\link[=s3_list_all]{s3_list_all()}}}

\item{site_url}{public base of the browse host}

\item{obj_url}{public base objects are fetched from}

\item{max_child_dirs}{a directory with more than this many SUBDIRECTORIES has
its children listed but not expanded into pages of their own}

\item{max_rows}{most rows rendered on one page, so a huge directory does not
emit a multi-megabyte document}
}
\value{
a data frame of \code{key} (the index.html object key) and \code{html}
}
\description{
A page is generated for EVERY directory, however many files it holds: the cost
of this index is the number of PAGES, not the number of objects, so a
directory of 20,000 files is still one page. Only a directory whose CHILDREN
are themselves numerous directories is collapsed - \verb{serve/model_cell/} is
~17,765 Hive partition directories, which would be ~17,765 pages nobody reads.
Those children are still listed with counts and labelled, so the reason is
visible rather than mysterious.
}
\concept{storage}
