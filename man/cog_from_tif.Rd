% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{cog_from_tif}
\alias{cog_from_tif}
\title{COG from a delivered GeoTIFF, values bit-exact (the \emph{native} representation)}
\usage{
cog_from_tif(
  src,
  out,
  band = 1,
  crop = TRUE,
  metadata = list(),
  overview = TRUE
)
}
\arguments{
\item{src}{path to the source GeoTIFF}

\item{out}{output path (\code{.tif})}

\item{band}{band index to publish (default 1)}

\item{crop}{crop to the band's non-NoData window (default \code{TRUE})}

\item{metadata}{named list written as \verb{-mo KEY=VALUE} items}

\item{overview}{build internal overviews (default \code{TRUE}, nearest)}
}
\value{
\code{out} (invisibly)
}
\description{
For a source that already IS a raster on the cell grid (AquaX), the native representation
is the delivered band itself, not a re-painting of the ingested cells: \verb{gdal_translate -of COG} of one band, cropped to the data window (a US-masked global raster is mostly NoData),
with the per-model scalars a reviewer needs (AUC / TSS / cutoff) written as GDAL metadata.
Complements \code{\link[=publish_cog]{publish_cog()}}, which paints \verb{(cell_id, val)} and is the \emph{model} representation.
}
\concept{publish}
