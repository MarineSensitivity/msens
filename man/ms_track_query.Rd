% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{ms_track_query}
\alias{ms_track_query}
\title{Time a query, log its shape, and re-raise any error}
\usage{
ms_track_query(session, event, params = list(), expr)
}
\arguments{
\item{session}{the Shiny \code{session} object}

\item{event}{event name, passed to \code{\link[=ms_event]{ms_event()}}}

\item{params}{named list of event parameters}

\item{expr}{the expression to time}
}
\value{
whatever \code{expr} returns
}
\description{
Wraps an expression so the Sheet records how long it took, how many rows it
returned, and whether it failed — in the dedicated \code{ms} / \code{n_rows} / \code{status}
/ \code{error} columns, which stay numeric and chartable rather than being buried
in the \code{params} JSON.
}
\details{
The result passes through untouched (including a lazy \code{dbplyr} table, whose
row count is deliberately NOT forced), and an error is re-raised after being
logged, so wrapping a call never changes behaviour.
}
\examples{
\dontrun{
d <- ms_track_query(session, "download_species_csv", list(area = hdr),
                    species_for_zone(con, "programarea_key", key))
}
}
\concept{analytics}
