% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_model.R
\name{cell_model_tile_sql}
\alias{cell_model_tile_sql}
\title{SQL expression for a cell's spatial tile id}
\usage{
cell_model_tile_sql(col = "cell_id", ncol = .CELL_GRID_NCOL)
}
\arguments{
\item{col}{SQL expression giving the cell id, e.g. \code{"c.cell_id"}}

\item{ncol}{grid width in columns (default v8's 7200; v7 is 3103 — see
\code{\link[=cell_grid_ncol]{cell_grid_ncol()}})}
}
\value{
character SQL expression
}
\description{
The single definition of the \code{cell_model} partition key, used by the release
notebook when writing and by \code{\link[=cell_model_tiles]{cell_model_tiles()}} when reading.
}
\details{
Note the integer division operator \verb{//}: DuckDB's \code{/} is FLOAT division, which
silently yields a distinct "tile" per cell (and therefore one partition per
cell) if used here by mistake.
}
\examples{
cell_model_tile_sql("cell_id")
cell_model_tile_sql("cell_id", ncol = 3103)   # v7 grid
}
\concept{calc}
