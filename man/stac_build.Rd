% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{stac_build}
\alias{stac_build}
\title{Build the full static STAC catalog for a version}
\usage{
stac_build(
  version = "v7",
  dir_out = NULL,
  cfg = NULL,
  nc_csv = NULL,
  con = NULL
)
}
\arguments{
\item{version}{data version (default "v7")}

\item{dir_out}{output directory for the catalog tree}

\item{cfg}{config from \code{\link[=stac_cfg]{stac_cfg()}} (defaults to \code{stac_cfg(version)})}

\item{nc_csv}{optional path to \code{nc_models.csv} for seasonal Items}

\item{con}{optional open DBI connection (else opens read-only via \code{\link[=sdm_db_con]{sdm_db_con()}})}
}
\value{
invisibly, the path to the root \code{catalog.json}
}
\description{
Connects to the version's SDM DuckDB, emits a Collection + model-surface Item
per dataset, optionally adds NCCOS seasonal Items from \code{nc_models.csv}, and
writes the tree (root Catalog -> version Collection -> dataset Collections ->
Items) under \code{dir_out}.
}
\concept{stac}
