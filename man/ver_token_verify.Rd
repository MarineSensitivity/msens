% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ver_token.R
\name{ver_token_verify}
\alias{ver_token_verify}
\title{Verify a version token; \code{NULL} unless it is genuine and unexpired}
\usage{
ver_token_verify(token, secret = ver_token_secret(), now = Sys.time())
}
\arguments{
\item{token}{the string from the page's hidden input (may be \code{NULL}/\code{""})}

\item{secret}{from \code{\link[=ver_token_secret]{ver_token_secret()}}}

\item{now}{the current time (injectable for tests)}
}
\value{
the version label, or \code{NULL} (missing, malformed, tampered, expired)
}
\description{
Verification is the \emph{candidate}: the caller must still resolve the returned
version through \code{\link[=atlas_resolve_ver]{atlas_resolve_ver()}} with the instance's
\code{\link[=atlas_allow_access]{atlas_allow_access()}}, so a token minted by another instance (the preview
host, for a restricted version) never widens what this process renders.
}
\concept{version}
