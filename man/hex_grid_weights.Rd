% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interp.R
\name{hex_grid_weights}
\alias{hex_grid_weights}
\title{Precompute IDW weights from a shared source grid to hex centroids}
\usage{
hex_grid_weights(
  con,
  hex_tbl,
  src,
  k = 8L,
  power = 2,
  where = NULL,
  radius_km = NULL,
  out_tbl = "hex_src_w",
  chunk = 5000000L
)
}
\arguments{
\item{con}{DuckDB connection (opened \code{bigint="integer64"})}

\item{hex_tbl}{hex table with \code{hex_id} BIGINT}

\item{src}{data.frame with \code{src_i} (integer) + \code{lon},\code{lat} source-grid points}

\item{k, power}{IDW parameters (default k=8, power=2)}

\item{where}{optional SQL predicate on \code{hex_tbl} (e.g. \code{"in_usa"}) to limit
the hexes weighted (a US-only score layer needs only US hexes)}

\item{radius_km}{optional great-circle cap (km): drop source neighbours beyond
this distance so distant points cannot extrapolate. Paired with a full source
grid (ocean + absent cells), this reproduces the zero-surround IDW fade — a
hex beyond ~1 cell of any present value has only absent (0) neighbours in
range and decays to 0. \code{NULL} = keep all k neighbours (default).}

\item{out_tbl}{base name for the weight tables (default "hex_src_w")}

\item{chunk}{hex rows per kd-tree batch (default 5e6)}
}
\value{
invisibly, \code{out_tbl}
}
\description{
One kNN (hex centroids -> source points, great-circle via a unit-sphere
kd-tree) whose weights are reused by every model on that source grid. Writes
\verb{<out_tbl>} (hex_id, src_i, w) with \code{k} rows per hex, and \verb{<out_tbl>_sum}
(hex_id, w_total) = the IDW denominator (sum of w over the k neighbours,
constant per hex regardless of model).
}
\concept{interp}
