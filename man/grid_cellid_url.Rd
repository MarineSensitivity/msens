% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{grid_cellid_url}
\alias{grid_cellid_url}
\title{Public URL of a grid's cell-id COG}
\usage{
grid_cellid_url(grid_id, base = atlas_base_url())
}
\arguments{
\item{grid_id}{\code{usa05} or \code{global05}}

\item{base}{atlas base URL from \code{\link[=atlas_base_url]{atlas_base_url()}}}
}
\value{
an https URL
}
\description{
The cell-id lookup published beside the release data, so a client can resolve
"which cell is at this point?" through the same titiler \verb{/cog/point} call it
uses for the layer value -- no local raster, and the id comes from the same
authority the tiles do. Written INT4U with \strong{no overviews}: cell ids are
categorical, and a resampled pyramid would average them into ids that do not
exist.
}
\concept{grid}
