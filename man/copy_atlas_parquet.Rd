% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet.R
\name{copy_atlas_parquet}
\alias{copy_atlas_parquet}
\title{COPY a DuckDB relation/query to Parquet with the standard v8 options}
\usage{
copy_atlas_parquet(
  con,
  sql,
  path,
  order_by = NULL,
  per_thread = FALSE,
  partition_by = NULL
)
}
\arguments{
\item{con}{open DuckDB connection}

\item{sql}{a SELECT (no trailing \verb{;}) OR a bare table name}

\item{path}{output file (or a directory when \code{partition_by}/\code{per_thread})}

\item{order_by}{optional ORDER BY column expression, e.g. \code{"mdl_key"}}

\item{per_thread}{write one file per thread into \code{path} (a dir); default FALSE}

\item{partition_by}{optional Hive-partition column(s), e.g. \code{"mdl_id"}}
}
\value{
\code{path}, invisibly
}
\description{
Emits \verb{COPY (<sql>) TO '<path>' (FORMAT parquet, PARQUET_VERSION V2, COMPRESSION zstd, ...)}. Two regimes:
\itemize{
\item \strong{unordered / partitioned} (default, or \code{partition_by}/\code{per_thread}):
drops insertion-order preservation so \verb{ROW_GROUP_SIZE_BYTES '80MB'} binds
(true ~80 MB row groups). Restores the prior setting afterward.
\item \strong{ordered} (\code{order_by} given): keeps insertion order for the serving
row-group zone-map pruning, so byte-sized groups can't be used — approximates
~80 MB with a row count instead.
}
}
\concept{parquet}
