% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_units}
\alias{app_units}
\title{The drawable scored spatial units of a release}
\usage{
app_units(con, manifest, geom_keys = list(), chosen = NULL)
}
\arguments{
\item{con}{a DBI connection to a release database}

\item{manifest}{a manifest from \code{\link[=manifest_build]{manifest_build()}} / \code{\link[=atlas_manifest]{atlas_manifest()}}, whose
\code{zones} rows carry \code{fld} and \code{pmtiles}}

\item{geom_keys}{optional named list \verb{zone_type -> character keys present in the published geometry}}
}
\value{
a list of unit objects for \code{boot.json}
}
\description{
A unit is offered iff (a) the manifest gives it PMTiles, (b) at least two of its
zones carry a composite \verb{score_\%} metric, and (c) at least two of those keys exist
in the published geometry. (c) is what excludes the whole-study-area rollups —
\code{USA} on v8, \code{FULL} on v7 — \strong{without hardcoding either name}; pass
\code{geom_keys} to apply it, and it is skipped (with the reason recorded) when the
geometry is not available to the builder.
}
\concept{app}
