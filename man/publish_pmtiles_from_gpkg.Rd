% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_pmtiles_from_gpkg}
\alias{publish_pmtiles_from_gpkg}
\title{Publish per-model PMTiles from an indexed GeoPackage}
\usage{
publish_pmtiles_from_gpkg(
  gpkg,
  layer,
  keys,
  dir_out,
  workers = NULL,
  redo = FALSE,
  minzoom = 0,
  maxzoom = 6,
  simplification = 10
)
}
\arguments{
\item{gpkg}{path to a GeoPackage whose \code{layer} has a character \code{mdl_key} column
(index it on \code{mdl_key} for speed: \verb{CREATE INDEX ... ON layer(mdl_key)})}

\item{layer}{gpkg layer name (also the tile \code{source_layer})}

\item{keys}{character \code{mdl_key}s to publish (each -> one file)}

\item{dir_out}{output directory (created); one \verb{\{slug(sp_id)\}.pmtiles} per key}

\item{workers, redo, minzoom, maxzoom, simplification}{as in \code{\link[=publish_pmtiles_models]{publish_pmtiles_models()}}}
}
\value{
a tibble \verb{(mdl_key, sp_id, file, mb)}, one row per model built or found
}
\description{
Like \code{\link[=publish_pmtiles_models]{publish_pmtiles_models()}} but reads each model's features \strong{on demand} from an
indexed GeoPackage (\verb{SELECT ... WHERE mdl_key = ...}) instead of a pre-loaded sf — so a
multi-GB source (full-resolution IUCN ranges are ~7 GB) never loads into memory at once,
and the per-model query is an index hit. Parallel (\code{furrr}) + resumable.
}
\concept{publish}
