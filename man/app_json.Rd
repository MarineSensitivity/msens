% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_json}
\alias{app_json}
\title{Serialize an \verb{app/} object exactly as it is published}
\usage{
app_json(x, pretty = FALSE)
}
\arguments{
\item{x}{an object}

\item{pretty}{pretty-print (default \code{FALSE}: these are machine objects)}
}
\value{
a single JSON string
}
\description{
One serializer so the bytes that were validated are the bytes that are written:
\code{auto_unbox} (a length-1 vector is a scalar, as the schemas declare),
\code{digits = NA} (full double precision — a rounded \code{rescale} changes a colour ramp),
\code{null = "null"} and \code{na = "null"} (absent is \code{null}, never the string \code{"NA"}).
}
\concept{app}
