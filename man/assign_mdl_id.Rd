% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdl_key.R
\name{assign_mdl_id}
\alias{assign_mdl_id}
\title{Assign the dense integer \code{mdl_id} for a set of models}
\usage{
assign_mdl_id(mdl_key, published = NULL)
}
\arguments{
\item{mdl_key}{character vector of model keys (need not be unique or sorted)}

\item{published}{optional data frame of the already-published registry with
columns \code{mdl_key} and \code{mdl_id}; ids for keys present here are preserved}
}
\value{
integer vector of \code{mdl_id}, parallel to \code{mdl_key}
}
\description{
\code{mdl_id} is the compact partition key of the serving surface
(\verb{serve/model_cell/mdl_id=\{id\}/}), so a titiler tile is a partition-pruned point
read. The \strong{stable public identifier stays \code{mdl_key}}: titiler resolves
\code{mdl_key -> mdl_id} from the published \code{model} registry, so \code{mdl_id} never
appears in a URL.
}
\details{
That indirection has a sharp edge. A plain \code{dense_rank(mdl_key)} is a
deterministic function of the model \emph{set}, which means \strong{adding any model
renumbers every model sorted after it}. Ingesting \code{gm} + \code{nc} into a released
version's \verb{dist/} — registered but deliberately not merged — moved 45,499 of
v8's 80,261 \code{mdl_id}s, and nothing would have failed: the registry and the
\verb{serve/} partitions would simply disagree, and titiler would serve the wrong
species' distribution for every model past \code{ch_nmfs}.

So ids are assigned \strong{against the published registry when one exists}: a
\code{mdl_key} already published keeps its id, and new keys are appended above the
current maximum in sorted order. Deterministic given
(published registry, model set), and it can never invalidate a published
partition. With \code{published = NULL} (a version that has never shipped) it falls
back to \code{dense_rank} over the sorted keys, which is what produced the ids of
every release to date.
}
\examples{
assign_mdl_id(c("b", "a", "c"))                                     # 2 1 3
assign_mdl_id(c("b", "a", "c"),
              data.frame(mdl_key = c("a", "c"), mdl_id = c(1L, 2L))) # 3 1 2
}
\concept{mdl_key}
