% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet.R
\name{write_atlas_parquet}
\alias{write_atlas_parquet}
\title{Write an atlas surface to Parquet (arrow path) with the standard v8 options}
\usage{
write_atlas_parquet(x, path, chunk_size = .atlas_pq$arrow_chunk)
}
\arguments{
\item{x}{a data.frame / tibble (typically \verb{mdl_key, cell_id, val})}

\item{path}{output \code{.parquet} path}

\item{chunk_size}{rows per row group (default from \code{.atlas_pq})}
}
\value{
\code{path}, invisibly
}
\description{
Parquet V2 (\code{version = "2.6"}), zstd compression, row groups of \code{chunk_size}
rows (arrow has no byte-based row-group control). Use in per-model / per-species
ingest loops where the data is an in-memory tibble (and often inside \code{mclapply} /
\code{furrr} forks). For DuckDB-resident data use \code{\link[=copy_atlas_parquet]{copy_atlas_parquet()}} instead —
both share the one option set in \code{.atlas_pq}.
}
\concept{parquet}
