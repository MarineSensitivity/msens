% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz.R
\name{drawn_features_sf}
\alias{drawn_features_sf}
\title{Parse mapgl's drawn-features Shiny input into \code{sf}}
\usage{
drawn_features_sf(x)
}
\arguments{
\item{x}{the raw value of \verb{input$<map_id>_drawn_features}: a character
GeoJSON string, a parsed list, or \code{NULL}}
}
\value{
an \code{sf} data frame in EPSG:4326 with one row per drawn feature, or
\code{NULL} when nothing is drawn or the payload is unusable
}
\description{
\code{mapgl}'s draw control pushes its FeatureCollection to
\verb{input$<map_id>_drawn_features}. \strong{What lands there has changed shape across
mapgl versions}: older builds sent \code{JSON.stringify(fc)} (a character
string), current builds (\verb{_mapglSyncDrawnFeatures}, mapgl >= 0.5.0) send the
object itself, which Shiny parses into a nested list. An app that tests for
only one of those forms silently sees "nothing drawn" — which is exactly how
the scores Report tab stopped recognizing drawn polygons.
}
\details{
This accepts BOTH forms so the apps cannot drift from mapgl again.
}
\examples{
drawn_features_sf(NULL)  # NULL — nothing drawn
}
\concept{viz}
