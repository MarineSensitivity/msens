% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz.R
\name{cell_tile_url}
\alias{cell_tile_url}
\title{Build an msens cell tile URL template}
\usage{
cell_tile_url(
  sql,
  colormap = "spectral_r",
  rescale = NULL,
  v = NULL,
  base = "https://titilecache.marinesensitivity.org"
)
}
\arguments{
\item{sql}{character(1); SELECT returning \code{cell_id} and \code{value} columns}

\item{colormap}{character; rio-tiler colormap name (default: "spectral_r")}

\item{rescale}{numeric length-2 \code{c(min, max)} for normalization; \code{NULL} lets
the server auto-compute from the SQL result on each tile request (prefer
a client-side value from \code{\link[=cell_stats]{cell_stats()}} for cache stability)}

\item{v}{character; optional cache-bust tag (e.g. DB build date)}

\item{base}{character; base URL of the titilecache service}
}
\value{
character(1) tile URL template
}
\description{
Returns an XYZ tile URL template (with \code{{z}/{x}/{y}} placeholders) for the
msens TiTiler factory. The SQL is canonicalized (whitespace collapsed) then
base64url-encoded. Consistent canonicalization is critical so repeated calls
with equivalent SQL produce identical URLs — Varnish keys on the full URL.
}
\concept{viz}
