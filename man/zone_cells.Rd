% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zone_set.R
\name{zone_cells}
\alias{zone_cells}
\title{Cells covered by each zone, on a given grid}
\usage{
zone_cells(ply, cellid_tif, key_col)
}
\arguments{
\item{ply}{an \code{sf} of zone polygons}

\item{cellid_tif}{path to that grid's cell-id COG}

\item{key_col}{column of \code{ply} holding the zone key}
}
\value{
a data frame with \code{zone_key}, \code{cell_id}, \code{pct_covered} (1-100)
}
\description{
The (zone × grid) intersection — which is a function of \strong{geometry and grid
alone}, not of any release. \code{score_zones.qmd} recomputes it per version, so
the six releases sharing \code{programarea_2026-03} each paid for the same
\code{exactextractr} pass; keyed on \verb{(zone_set_key, grid_id)} it is computed once.
}
\details{
Reads the grid's \strong{cell-id COG}, whose pixel VALUES are cell ids (a lookup
image — see \code{grid.R}), so the returned ids are in that grid's id-space
whatever frame the raster itself uses.

Semantics deliberately match the per-version implementation this replaces, so
relocating it cannot move any score: coverage is rounded to whole percent and
anything rounding to zero is dropped.
}
\concept{zone_set}
