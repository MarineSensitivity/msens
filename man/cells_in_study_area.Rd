% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{cells_in_study_area}
\alias{cells_in_study_area}
\title{Restrict a cell set to the release's study area}
\usage{
cells_in_study_area(con, cells)
}
\arguments{
\item{con}{a DBI connection to a release database}

\item{cells}{a tibble with \code{cell_id} and \code{pct_covered}}
}
\value{
the same tibble, keeping only in-study-area cells (column order and
\code{pct_covered} preserved), ordered by \code{cell_id}
}
\description{
D7b: a custom place is clipped to the cells that actually exist in the release's
\code{cell} table with \code{coalesce(in_usa, TRUE)}. Everything downstream -- scores,
species, area, N cells -- then runs on ONE cell set, and land and foreign waters
never enter a coverage-blended score as zeros.
}
\details{
\code{coalesce(in_usa, TRUE)}, not \code{in_usa}: v1-v7 have no such column, and a release
whose \code{cell} table is already the study area must not be emptied by its absence.
}
\concept{place}
