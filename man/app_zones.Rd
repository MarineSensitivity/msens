% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_zones}
\alias{app_zones}
\title{Per-zone summaries, so a report on a Program Area never loads a cell}
\usage{
app_zones(con, flds = NULL, chosen = NULL)
}
\arguments{
\item{con}{a DBI connection}

\item{flds}{zone fields to summarise (default: every \code{fld} in \code{zone})}
}
\value{
a named list \verb{zone_type -> list of zone objects}
}
\description{
\code{n_cells} and the coverage-weighted \code{area_km2} come from \code{zone_cell} joined to
\code{cell}; \code{metrics} is that zone's \code{zone_metric} keyed by \code{metric_key} (never by
\code{metric_seq}, which is dropped and recreated every run). \code{coverage} carries the
v7.1-only \verb{\{component\}_coverage} rows when the release has them and is \code{NULL}
otherwise — \strong{optional by presence}, like the \code{methods} table.
}
\details{
A component with no row is ABSENT, not zero: that is how the release records "not
reportable", and the app must render it as such.
}
\concept{app}
