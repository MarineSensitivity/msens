% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{parse_qmd_frontmatter}
\alias{parse_qmd_frontmatter}
\title{Parse \verb{msens:} frontmatter from pipeline notebooks}
\usage{
parse_qmd_frontmatter(workflows_dir = here::here(), pattern = "*.qmd")
}
\arguments{
\item{workflows_dir}{directory holding the pipeline \code{.qmd}s (default
\code{\link[here:here]{here::here()}})}

\item{pattern}{glob for notebooks (default \code{"*.qmd"})}
}
\value{
a tibble with columns \code{qmd_file}, \code{target_name}, \code{workflow_type},
\code{dependency} (list column), \code{output}
}
\description{
Globs the \code{.qmd} files in \code{workflows_dir}, reads the YAML frontmatter of each
(the block between the first two \verb{---} fences), and keeps only those carrying
a top-level \verb{msens:} key. Returns one row per pipeline notebook with the
fields needed to wire the \code{targets} DAG.
}
\details{
The \verb{msens:} block vocabulary:
\describe{
\item{target_name}{the \code{targets} node name (a legal R symbol)}
\item{workflow_type}{one of \code{grid}, \code{ingest}, \code{merge}, \code{score}, \code{publish},
\code{release}, \code{test}}
\item{dependency}{list of upstream \code{target_name}s, or \verb{[auto]} to depend on
every \code{grid} + \code{ingest} target}
\item{output}{the file/dir the notebook produces (tracked via
\code{format = "file"}; may contain a \code{*} glob)}
}
}
\examples{
\dontrun{
wf <- parse_qmd_frontmatter("~/Github/MarineSensitivity/workflows")
dplyr::filter(wf, workflow_type == "ingest")
}
}
\concept{workflow}
