% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{app_id_chr}
\alias{app_id_chr}
\title{Normalise an id column to the text every generation must publish}
\usage{
app_id_chr(x, what = "id")
}
\arguments{
\item{x}{an id vector (numeric, integer, integer64 or character)}

\item{what}{name used in the error message}
}
\value{
a character vector
}
\description{
The R-side twin of the SQL cast. An id reaches a published object by several
routes — a \code{SELECT} this package writes, a \verb{SELECT *} of a precomputed table, a
column already read into R — and only the first was covered, so v7's
\code{zone_taxon.parquet} still carried \code{taxon_id} as a DOUBLE and published
\code{"22725044.0"} while \code{taxon.parquet} beside it said \code{"22725044"}. One contract,
two spellings, and every join between them empty.
}
\details{
Whole numbers become digits only; \code{NA} stays \code{NA}; a trailing \code{.0} on a string is
stripped (it means whole); anything genuinely fractional is an \strong{error}, never a
rounded neighbour — see \code{\link[=app_taxon_table]{app_taxon_table()}}.
}
\examples{
app_id_chr(c(137162, NA))          # "137162" NA
app_id_chr(c("22725044.0", "7"))   # "22725044" "7"
}
\concept{app}
