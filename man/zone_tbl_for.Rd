% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_bundle.R
\name{zone_tbl_for}
\alias{zone_tbl_for}
\title{Zone table name for a spatial unit}
\usage{
zone_tbl_for(con, fld, ver = NULL)
}
\arguments{
\item{con}{a DBI connection to a release database}

\item{fld}{zone field, e.g. \code{"programarea_key"}}

\item{ver}{version label, used only to prefer a suffixed name}
}
\value{
a table name, or \code{NA_character_} when the release has no such unit
}
\description{
\verb{SELECT DISTINCT tbl FROM zone WHERE fld = ...}, preferring a name ending in the
release's own suffix. Never \code{glue("ply_subregions_2026_{ver}")}: v1 and v2 zone
tables are UNSUFFIXED, so the guessed name matched nothing, the subregion cache
was written empty and never healed, and the choropleth came up blank with an
\code{InfM/-InfM} legend from \code{range(numeric(0))}.
}
\concept{app}
