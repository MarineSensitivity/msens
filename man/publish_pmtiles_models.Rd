% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_pmtiles_models}
\alias{publish_pmtiles_models}
\title{Publish per-model PMTiles — one file per \code{mdl_key}}
\usage{
publish_pmtiles_models(
  x,
  dir_out,
  layer,
  workers = NULL,
  redo = FALSE,
  minzoom = 0,
  maxzoom = 6,
  simplification = 10
)
}
\arguments{
\item{x}{an sf with a character \code{mdl_key} column (features may repeat a key)}

\item{dir_out}{output directory (created); one \code{.pmtiles} per model}

\item{layer}{tile layer name (the app's \code{source_layer})}

\item{workers}{parallel workers (default \code{max(1, detectCores() - 2)})}

\item{redo}{rebuild files that already exist (default \code{FALSE})}

\item{minzoom, maxzoom, simplification}{passed to \code{\link[=publish_pmtiles]{publish_pmtiles()}}}
}
\value{
a tibble \verb{(mdl_key, sp_id, file, mb)}, one row per model built or found
}
\description{
Splits an sf carrying a \code{mdl_key} column into \strong{one PMTiles per model}, written to
\verb{\{dir_out\}/\{slug(sp_id)\}.pmtiles} (\code{sp_id} = \code{mdl_key} after the first \code{|}). Each file
holds a single model, so the species app needs no client-side filter and — crucially —
low-zoom tiles never collide across species. Packing thousands of overlapping global
ranges into one per-dataset archive overflows the z0–z2 world tiles past maplibre's
per-tile budget, so it silently drops them (a selected range shows "no polygon at all"
at global zoom); a per-model file tiles to a few KB and always renders. Mirrors the
per-model AquaMaps COGs. Parallel (\code{furrr}) + resumable (skips existing unless \code{redo}).
}
\concept{publish}
