% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{zone_scores}
\alias{zone_scores}
\title{Every zone-level score of one spatial unit, across all metrics}
\usage{
zone_scores(con, fld = "programarea_key", zone_set_key = NULL, label = "db")
}
\arguments{
\item{con}{a DBI connection to a release's \code{sdm.duckdb}}

\item{fld}{zone field to read (default \code{"programarea_key"})}

\item{zone_set_key}{optional \verb{\{zone_type\}_\{YYYY-MM\}} to pin the spatial unit;
errors if a connection carries the column but not that value}

\item{label}{version label for error messages (default \code{"db"})}
}
\value{
a tibble of \code{zone_key}, \code{metric_key}, \code{score}
}
\description{
The long-form reader that feeds \code{\link[=zone_score_delta]{zone_score_delta()}}: one row per
(zone, metric) for the zones of \code{fld} — Program Areas by default — in one
release's \code{sdm.duckdb}. Schema-adaptive like \code{\link[=pra_score_delta]{pra_score_delta()}} (\code{value} in
v1–v7, \code{val} in v8+) and pinnable to a zone set the same way, so a v6 database
(no \code{zone_set_key} column) and a v9 database can be read with one call each
and compared with \code{\link[=zone_score_delta]{zone_score_delta()}}.
}
\concept{validate}
