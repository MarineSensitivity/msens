% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place.R
\name{geojson_sfc}
\alias{geojson_sfc}
\title{Build an \code{sfc} from parsed GeoJSON coordinate arrays}
\usage{
geojson_sfc(x)
}
\arguments{
\item{x}{a parsed GeoJSON geometry (\code{jsonlite::fromJSON(simplifyVector = FALSE)}),
of type \code{Polygon} or \code{MultiPolygon}}
}
\value{
an \code{sfc} in EPSG:4326
}
\description{
Deliberately NOT \code{sf::st_read()} on a GeoJSON string: that goes through GDAL and
then s2, which rejects a ring carrying a duplicate vertex — something a
coordinate-rounded coastline produces routinely, and something a planar cell
intersection does not care about at all. Reading the arrays directly also means
the fixture's \code{geometry} is exactly the numbers a TypeScript twin parses, with no
library in between to normalise them differently.
}
\concept{place}
