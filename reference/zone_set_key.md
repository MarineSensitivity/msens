# Compose (and validate) a zone-set key

Compose (and validate) a zone-set key

## Usage

``` r
zone_set_key(zone_type, vintage)
```

## Arguments

- zone_type:

  one of `planarea`, `programarea`, `ecoregion`, `subregion`

- vintage:

  `YYYY-MM`

## Value

`"{zone_type}_{YYYY-MM}"`
