# Build the SDM properties block shared by Collections and Items

Build the SDM properties block shared by Collections and Items

## Usage

``` r
stac_sdm_props(ds)
```

## Arguments

- ds:

  one-row data.frame from the `dataset` table

## Value

named list of core + optional sdm: fields
